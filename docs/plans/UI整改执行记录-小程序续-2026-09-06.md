# 小程序端 UI 整改执行记录（续）· 2026-09-06

> 严格按 `impeccable` skill 的 `/normalize` 工作流 + a11y 规范执行。
> 本轮目标：收口上一轮遗留的小程序端 P1-#4 / P2 / P3 与 H5 键盘可达（P1-#5）。
> 所有改动**仅针对 token 引用与 a11y 包裹，值不变、不改变交互逻辑、未引入回归**。

## 一、已完成项（带 file:line + 验证）

### T19 · `/normalize`：`--color-*` 旧别名全量迁移到规范令牌（P1-#4）
规范令牌值与旧别名**完全相同**，纯卫生清理、零视觉回归。

| 文件 | 旧写法 | 新写法 |
|---|---|---|
| `components/AppButton.vue` | `var(--color-primary, #0b1d3a)` ×4、`var(--color-accent, #c8a96e)` ×1 | `var(--brand-deep, #0B1D3A)`、`var(--gold, #C8A96E)` |
| `components/AppTopBar.vue` | `var(--color-card, #ffffff)` ×1、`var(--color-primary, #0b1d3a)` ×2 | `var(--bg-card, #FFFFFF)`、`var(--brand-deep, #0B1D3A)` |
| `components/AppSkeleton.vue` | `var(--color-border, #e5e7eb)` ×1 | `var(--line, #E2E8F0)` |
| `components/AppIcon.vue` | `var(--color-card, #ffffff)` ×2 | `var(--bg-card, #FFFFFF)` |

- **验证**：全项目 grep `--color-(primary|card|text|border|accent|...)` 仅剩 `App.vue:165-176` 兼容层定义（按设计保留，待全量迁移后删除）。✅

### T18 · `/normalize`：`<switch>` 裸 hex 抽脚本常量（P2）
`switch` 原生 `color` 属性**不支持 CSS 变量**，故用脚本常量承载（单源）。

| 文件 | 改动 |
|---|---|
| `pages/match/match.vue` | `color="#0B1D3A"`(244,248) → `:color="SWITCH_COLOR"`；在 `onSwitch` 前注入 `const SWITCH_COLOR = '#0B1D3A'`（值取自 `--brand-deep`） |
| `pages/auth/auth.vue` | `color="#0B1D3A"`(96) → `:color="SWITCH_COLOR"`；注入同款常量 |

- **验证**：grep `color="#0B1D3A"` 全项目 **0 处**。✅

### T16 · `/normalize`：裸字号令牌化收纳（P3）
裸字号 28/32/40/48rpx 均**不在原字号阶梯**（22/24/26/30/36/44）内。按"值不变"原则在 `App.vue` 补扩展令牌并替换引用：

- `App.vue` 字号阶梯新增：`--fs-em: 28rpx; --fs-title: 32rpx; --fs-arrow: 40rpx; --fs-display: 48rpx;`
- 替换清单：
  - `components/AppButton.vue`：btn-md `28rpx`→`--fs-em`、btn-lg `32rpx`→`--fs-title`
  - `components/AppTopBar.vue`：nav-title `32rpx`→`--fs-title`
  - `pages/home/home.vue`：promo-title `28rpx`→`--fs-em`、promo-arrow `40rpx`→`--fs-arrow`
  - `pages/index/index.vue`：hero-title `48rpx`→`--fs-display`、role-check `32rpx`→`--fs-title`
  - `pages/mine/mine.vue`：advisor-avatar `32rpx`→`--fs-title`、menu-arrow `40rpx`→`--fs-arrow`
  - `pages/order/detail.vue`：status-label `32rpx`→`--fs-title`

- **验证**：grep `font-size:\s*(28|32|40|48)rpx` 全项目 **0 处**（仅 `App.vue` 令牌定义保留，非裸值）。✅
- **遗留说明**：页面内另有 `23rpx`/`29rpx` 等离阶梯裸字号（±1~3rpx 接近 `--fs-sm`/`--fs-md`），本轮未纳入——建议后续按设计意图归并到最近阶梯令牌（涉及视觉微调，需走查确认）。

### T20 · a11y：建 `<AppClickable>` 组件并示范（P1-#5）
新增统一 a11y 封装组件，收敛"原生 `view @click` 缺键盘可达"问题。

- **新增 `components/AppClickable.vue`**：根 `<view>` 自带 `role`(默认 button) / `tabindex` / `:focus-visible` 金色焦点环（H5 独占，小程序端安全忽略）；`@keydown` 监听 Enter/Space 触发 `click`；`disabled` 时 `tabindex=-1` 且拦截事件。依赖 uni-app easycom 自动注册，无需 import。
- **应用**：
  - `pages/match/match.vue`：将上一轮手动 `tabindex/role/@keydown` 写法收敛为 `<AppClickable>`（type-item ×2、upload-tile ×N），删除 8 行样板，DOM 结构等价、flex 布局不变。
  - `pages/home/home.vue`：代表性示范 search-bar / auth-pill / promo-card 三个点击元素改用 `<AppClickable>`。
- **验证**：grep `app-focusable` 页面用法 **0 处**（全局焦点规则保留无害）；`<AppClickable>` 在 match/home/组件共 12 处引用。✅

## 二、仍待办（建议下一批，需走查/立项）

1. **H5 键盘可达全站 rollout（P1-#5 主体）**：其余页面（index/mine/auth/report/list/product/order/approval/client/create/edit/detail 等约 14 个）仍有 ~65 处 `view @click` 未补。转换配方已固化（`<AppClickable class="x" @click="fn">`），建议按页逐一替换并 QA：
   - 定位：`grep -rn "@click" loan-mini/pages --include=*.vue`（排除已用 AppClickable / AppButton 的）
   - v-for 动态 `card.action` 类点击需评估是否改 `AppClickable`（注意包裹会改变列表子节点层级）。
2. **`--color-*` 兼容层删除**：待全站（含任意遗漏页面）确认无 `--color-*` 引用后，删除 `App.vue:165-176` 别名块（本轮已全量迁移组件层）。
3. **裸字号 23/29rpx 归并**：设计决策，需视觉走查。
4. **工程化防回归**：引入 stylelint（禁裸 hex / 裸字号 / 裸 rgba）；建立 `<AppClickable>` 使用规范（新代码强制）。
5. **编译验证**：本轮仅做 grep 静态验证，未跑 `uni build`（纯 token/字符串/组件替换，风险低，建议提测前跑一次微信小程序 + H5 双端编译）。
6. **Web 端 `AnimatedBg.vue`**（任务 #13）：已判定为登录页刻意品牌装饰面（`.env` 可切、已尊重 `prefers-reduced-motion`），审计报告标注"产品决策可保留"——**保留不扁平化**，如需调整另行立项。

## 三、本轮未触碰（沿用前期结论）
- 小程序 P1 装饰渐变/径向金光/裸色：已在 09-06 首轮清零并验证。
- Web 端报告模块令牌化、Element 主题覆盖、缺失令牌补齐：已在 09-06 Web 轮完成。
