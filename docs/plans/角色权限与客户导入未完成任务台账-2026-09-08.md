# 角色权限与客户导入未完成任务台账（2026-09-08）

## 状态说明

- `已确认`：业务口径已经由用户确认。
- `已实现待回归`：代码或数据库已完成，尚待完整角色场景联调。
- `开发中`：已有部分实现，不能视为完成。
- `未开始`：尚未落代码。

## 一、角色确认进度

| 角色 | 确认状态 | 后续动作 |
|---|---|---|
| 客户 | 已确认 | 按本人报告、本人工单回归 |
| 顾问 | 已确认 | 公海直领、本人释放、转分配申请、新增产品/线索回归 |
| 部门经理 | 已确认并补充批量能力 | 补 Web 我的/团队客户分栏及批量按钮 |
| 运营管理员 | 已确认 | 禁止跨团队直分、任意团队回收、系统权限管理；保留运营业务能力 |
| 老板 | 已确认 | 全部权限；审批运营短信/报告模板 |
| 超级管理员 | 已确认 | 全部权限；审批运营短信/报告模板 |
| 渠道合作方 | 已确认 | 沙箱隔离；仅本人产出数据；禁止认领/分配/匹配/工单/批量导入 |

## 二、已实现待回归

1. 客户公司公海 `ENTERPRISE`、团队公海 `TEAM` 字段和索引已执行到 Nacos prd 数据库。
2. 顾问直接认领公司公海/本人团队公海；已有他人归属走转分配申请。
3. 跨团队转分配仅老板或超级管理员审批；运营、部门经理禁止。
4. 部门经理本团队直接分配、回收，以及批量分配/批量回收后端接口。
5. 所有公司员工批量认领可见公海客户，最多 500，整批事务回滚。
6. 新增线索按手机号、企业信用代码、个人身份证号硬查重；同名仅提醒。
7. 小程序精确权限集合通过 `/api/mini/me.roleInfo.permissions` 下发。
8. 批量导入角色守卫与产品/线索 Excel 模板下载。

## 三、开发中

### 1. 批量导入导出

- 仅老板、超级管理员可操作。
- 已完成：角色守卫、POI 依赖、模板动态生成、模板下载接口。
- 已完成：上传大小校验、首个 Sheet 通用解析、最多 5000 行预览接口（不写库）。
- 未完成：产品/线索处理器、数据库任务表/明细表、正式导入事务、进度查询、结果导出、Web 页面。
- 去重要求：文件内去重；数据库批量查重；产品按银行+产品名+客群；线索按手机号/信用代码/身份证号；同名只警告。

### 4. 运营内容审批

- 已完成：运营保存短信模板时强制禁用；保存报告模板时进入 `PENDING_APPROVAL`；启停/发布接口仅老板和超级管理员可调用。
- 已完成：统一审批中心新增 `SMS_TEMPLATE`、`REPORT_TEMPLATE` 类型；审批记录、待审计数、驳回意见、Web/小程序审批页签、审批结果站内通知均已接入；数据库迁移已执行到 Nacos prd。

### 2. 客户视图与团队范围

- 已完成 Web 分栏：我的客户、团队客户、公司客户、公司公海、团队公海。
- `scope=MY/TEAM/ALL/COMPANY_SEA/TEAM_SEA` 后端入口已接入；待补单元测试并核对所有调用方。
- 团队客户必须包含部门经理本人和本部门顾问，不得依赖前端传任意 owner 编码越权。

### 3. 小程序权限空态

- 产品、审批、报告、工单、客户录入已接入精确权限空态。
- 首页快捷入口、TabBar、我的页审批入口统一使用后端下发权限键；顾问不再显示审批入口。
- 请求层对 HTTP 403/业务权限码统一标记 `permissionDenied`，页面可据此展示无权限空态。
- 已完成：产品编辑页入口与保存按钮级守卫、审批类型 Tab 的角色过滤。
- 尚待完成：真实角色回归与后端审批数据范围回归。

## 四、未开始

1. 公海认领 Redis 短锁，与数据库乐观更新形成双保险。
2. 客户持有上限、每日认领上限配置。
3. 批量操作任务记录、失败明细和审计回退字段。
4. 分配流水保留最近 N 条及过期清理任务。
5. 全七角色 Web/H5/小程序/后端真实数据联动回归。
6. 菜单、按钮权限、接口权限和数据范围四层矩阵自动化测试。

## 五、验证要求

- 使用 ARM64 JDK8。
- 数据库配置只读取 Nacos `prd`，不启动本地 MySQL/Nacos/Redis。
- 每批修改集中编译、测试、构建，不按单个 Bug 重启。
- 数据库 DDL 幂等执行并回读字段、索引和数据分布。
