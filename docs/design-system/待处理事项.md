# 待处理事项 · 墨金（Ink & Gold）设计系统落地接入

> 本清单由 WorkBuddy 编写，作为「先写一遍」的基准版本。
> 用途：将已确认归档的设计系统（位于 `docs/design-system/`）落地到两端前端源码。
> 前置条件：设计方向、令牌、图标库、组件规范、原型均已确认并归档（无需重新确认）。

---

## 一、背景与目标

- **已完成**：设计系统「墨金 Ink & Gold」已确认，交付物在 `docs/design-system/`：
  - `DesignSystemManifest.md` — 统一语义令牌（墨蓝 + 暖金，明暗双主题映射）
  - `ComponentSpec.md` — 组件清单与 full 状态
  - `QAReport.md` — 质量检查（已通过，3 条 P2 建议）
  - `DeveloperGuide.md` — Web / Mini 落地步骤与旧图标名映射
  - `code/icons.js` — 36 枚原创 SVG 图标（单一事实来源）
  - `code/AppIcon.web.vue` / `code/AppIcon.mini.vue` — 双端图标组件
  - `prototype/design-showcase.html` — 可交互原创图预览
- **待办**：上述设计尚未改动 `loan-web`、`loan-mini` 源码。本事项即把设计「落地」。
- **目标**：消除图标大小/线宽/着色不一致、样式令牌互不相通两类核心问题，且换肤只改一处。

## 二、范围与边界

| 项 | 纳入 | 排除 |
|---|---|---|
| 端 | loan-web（管理端）、loan-mini（小程序） | loan-service（Java 后端，非前端） |
| 改动 | 样式令牌化、图标统一、字体/主题 | 业务逻辑、接口、路由结构 |
| 风险动作 | 替换硬编码颜色、替换内联 SVG | 删除业务组件、改接口字段 |

## 三、待处理事项清单

### A. loan-web（Vue3 + Element Plus + Vite）
- [ ] **A1 令牌接入**：`src/styles/index.css`（约 1157 行）中硬编码 hex（如 `#16203A`/`#D9A441` 等 magic number）替换为 `DesignSystemManifest.md` 的语义令牌；保留 `--loan-*` 别名做向后兼容或统一重命名。
- [ ] **A2 图标收敛**：引入 `code/AppIcon.web.vue`；替换散落在 **23 个 .vue** 文件中的手写内联 `<svg>`（尺寸 12/14/16/18/20、线宽 1.7/1.8/2/2.4 混乱）；统一调用 `AppIcon`。
- [ ] **A3 EP 内置图标处理**：`el-alert show-icon`、`el-button icon` 等 Element Plus 内置图标，在不破坏功能前提下逐步替换为原创 `AppIcon`，保证视觉一致。
- [ ] **A4 暗色主题**：应用工作台原型（design-showcase 第 4 屏）的暗色承载层、卡片、表格、趋势图样式；确保 `var()` 随主题联动。
- [ ] **A5 字体替换**：全局字体改为 `Space Grotesk`（西文）+ `Noto Sans SC`（中文），移除 Inter/Roboto 痕迹。
- [ ] **A6 自测**：图标尺寸/线宽统一、换肤颜色联动、无控制台报错。

### B. loan-mini（uni-app Vue3 小程序）
- [ ] **B1 令牌对接**：`--brand-*` 对齐统一语义令牌；补充 Mini 端缺失的间距/圆角标尺。
- [ ] **B2 图标着色修复（关键）**：引入 `code/AppIcon.mini.vue`，通过 **THEME 真实色值运行时注入** 替代写死的 `#2443C2` / TabBar `rgba(26,35,54,.55)`；解决小程序 SVG 不解析 `var()` 导致图标变实心圆/消失的历史 bug。
- [ ] **B3 字体/间距/圆角令牌化**：统一页面级间距与圆角，去除散落 magic number。
- [ ] **B4 TabBar 联动**：底部 TabBar 图标与文字激活态随换肤切换为暖金。
- [ ] **B5 真机自测**：微信开发者工具 + 真机预览，验证图标渲染、主题切换无异常。

### C. 双端验收
- [ ] **C1 QA 复检**：按 `QAReport.md` 的 5 项检查逐项复检，重点确认「设计令牌合规」「装饰合理」。
- [ ] **C2 视觉走查**：双端图标统一、配色一致、明暗主题正确。
- [ ] **C3 遗留跟进**：处理 3 条 P2 建议（如有），输出复检结论。

## 四、验收标准

1. **令牌单一来源**：换肤时仅修改一处令牌定义，双端（及图标）同步变化。
2. **图标统一**：全部业务图标来自 `code/icons.js` 单一来源，统一 24×24、线宽 1.75、圆角端点、`currentColor` 着色；Web 随 `var()` 联动，Mini 随 THEME 注入联动。
3. **无硬编码颜色**：`loan-web/src/styles/index.css` 与 `loan-mini` 全局样式中不再出现游离 hex（令牌未覆盖的极少特例需注释说明）。
4. **零回归**：原有功能、路由、接口不受影响；构建通过、无控制台报错。

## 五、关键风险与注意事项

- **index.css 体积大**：1157 行含大量重复硬编码，建议增量替换 + 全局搜索游离 hex，避免一次性大改引入回归。
- **Mini var() 陷阱**：小程序 SVG `stroke` 不解析 `var()`，必须用真实色值注入（见 `AppIcon.mini.vue` 的 THEME 方案），切勿直接复用 Web 的 `var()` 写法。
- **图标名映射**：`DeveloperGuide.md` 提供旧图标名 → 新 `AppIcon` 名映射，迁移时对照使用，避免漏改。
- **分阶段提交**：建议 A/B/C 三段分别提 PR，便于 review 与回滚。

## 六、参考输入物（已归档，勿改）

`docs/design-system/` 下：DesignSystemManifest.md、ComponentSpec.md、QAReport.md、DeveloperGuide.md、code/、prototype/design-showcase.html

## 七、已落地进展（2026-09-10 更新）

> 以下为相对于本基准清单，已在近期会话中实际落地的部分，供接手工友对齐，避免重复劳动。

### 已完成（超出原清单范围或提前完成）
- **B2 图标着色修复**：`components/AppIcon.vue` 已用 THEME 真实色值运行时注入（浅 `#1A2336` / 暗 `#E8EDF5`），base64 data URI 渲染，修复图标消失/实心圆 bug。✅
- **B4 TabBar 联动**：`components/TabBar.vue` 激活态随主题转暖金 `#E0AE4E`，文案已回正为 首页/匹配/报告/服务单/我的。✅
- **小程序首页重做**：`pages/home/home.vue` 按墨金效果图整页重写（问候头 + 深色 AI 卡 + 四宫格 + 报告列表）。✅（属设计落地外的产品侧改动）
- **合规整改**：贷款/融资→资金、审批→审核，三端零命中（含 `loan-web/dist` 重建）。✅（原清单未覆盖，属独立收尾）
- **提交**：`8f587f9` 墨金双主题 + 合规整改（66 文件）。✅

### 仍待办（原清单主体，尚未落地）
- **A1~A6（loan-web 令牌/图标/字体/暗色）**：`src/styles/index.css` 仍有大量硬编码 hex，23 个 .vue 内联 SVG 未收敛，字体未替换。❌
- **B1/B3（Mini 令牌/间距圆角）**：`--brand-*` 未完全对齐语义令牌，间距圆角仍有 magic number。❌
- **C1~C3（双端验收）**：未做 QA 复检与视觉走查。❌

### 新增阻塞项
- `loan-web/dist_old/`、`loan-mini/dist/build/h5_old/` 残留（沙箱批量删除守卫拦截），需分批或手动 `rm -rf`。

## 七、页面重构待处理（2026-09-10）

以下事项已确认纳入后续重构，当前提交仅保存基线与已完成的登录交互，不视为全部完成：

> **唯一参考原型**：`docs/prototypes/redesign-role-business-review.html`（三端分角色业务原型复核版）。UI 风格确认稿 v2（`redesign-review-v2.html`）已于本轮移除，以免多份原型造成参考歧义；如需回溯见提交 `11e66c0`。

- [ ] **P1 Web 页面重构**：按角色重新梳理工作台、客户、公海、审批、工单和消息中心的布局与按钮级权限。→ **未做**：属 Web 管理端，02-红线「助手不碰 loan-web」，须由用户执行。
- [x] **P2 H5 页面重构**：与小程序保持统一视觉令牌、登录结构和交互状态；手机号验证码登录采用独立切换页。→ **已完成**：令牌统一（D70）；验证码登录拆为独立页 `pages/auth/phone-login.vue`，落地页仅留切换入口，`index.vue` 内联表单与死代码已移除。
- [x] **P3 小程序页面重构**：按角色重做首页、我的客户、报告、工单、产品/线索录入及空态/骨架屏。→ **已完成**：首页 7 角色化 + TabBar 角色结构（D70）；本轮补 `mine` 消息通知/业务录入、`approval` 补 TabBar + u-shell + 底部安全区；报告/工单/产品/线索录入页骨架屏与空态齐备。**注**：小程序不承载「我的客户」列表页（属 Web，见 08 矩阵 / D50），无 mini 页的入口提示「Web 管理端使用」。
- [ ] **P4 三端视觉验收**：统一间距、字号、图标、按钮、单行文案、协议勾选和合规提示，并完成真机与浏览器回归。→ **部分**：两端构建通过、令牌/图标/文案已统一；真机与浏览器人工回归待用户执行。
