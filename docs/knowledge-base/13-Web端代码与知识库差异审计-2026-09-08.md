# Web 端代码与知识库差异审计（2026-09-08）

> 范围：`loan-web/src`。本文件记录审计与修复进度。依据 D39、D49、D53、D60、D63、D67、D68 及 `loan-web-dev`、`loan-web-ui`、`loan-web-components` 规范逐项核对。

> 2026-09-08 修复进度：WEB-01、WEB-02、WEB-07、WEB-15 已完成；WEB-03～06 暂缓，原因是后端创建接口当前仍要求客户端提交业务编码，需先完成后端生成编码契约后再隐藏输入，避免新增功能回归。WEB-13 需在客户表格源码结构调整时一并接入排序回调。

## 一、P0/P1 缺陷

| 编号 | 优先级 | 路径与行号 | 现象 | 根因 | 建议 |
|---|---|---|---|---|---|
| WEB-01 | P0 | `src/views/approval/ApprovalCenter.vue:397` | 下载申请资料下拉标签拼接 `工单 ${a.orderNo}`，客户端直接展示工单业务号，违反 D68。 | 复用附件对象时将内部关联号用于可读标签，未使用客户名/资料名替代。 | 后端批量补齐客户姓名、资料类型；前端标签只显示“客户姓名 · 资料类型 · 文件名”，业务号只保留 value/请求参数。 |
| WEB-02 | P0 | `src/views/client/ClientProfile.vue:142-143` | 分配/跟进历史在姓名缺失时回退显示 `fromStaffCode/toStaffCode`，违反 D68。 | 名称装配失败时采用编码兜底。 | 后端 `BusinessNameService` 批量补齐姓名；缺失统一显示“姓名待补充”，禁止编码回退。 |
| WEB-03 | P0 | `src/views/rule/RuleTemplateList.vue:52,75,84,104,129` | 模板编码、字段编码在表单、字段列表、导入选择器中可见，违反 D68 的客户端零展示。 | 管理端旧页面仍把业务编码当作用户可理解字段。 | 表单改为模板名称/字段名称；编码仅内部提交与行键，导入选项显示字段名称和类型。 |
| WEB-04 | P0 | `src/views/product/ProductList.vue:159,602` | 产品编码仍作为可编辑表单标签和必填字段展示，违反 D68。 | 产品编码生成/维护模型尚未完全从展示模型分离。 | 产品编码改为后端生成并隐藏；编辑页只展示产品名称、银行、客群、额度、期限、状态。 |
| WEB-05 | P0 | `src/views/template/ReportTemplateList.vue:47,110` | 报告模板编码在新增/编辑页面可见并要求输入，违反 D68。 | 报告模板页面仍沿用旧手工编码设计。 | 模板编码改后端生成；客户端只展示模板名称、版本、适用客群、状态。 |
| WEB-06 | P0 | `src/views/sms/SmsCenter.vue:102,192` | 短信模板编码在管理页面可见并要求输入，违反 D68。 | 业务编码与展示字段耦合。 | 后端生成模板编码；前端移除编码输入和列表列，操作按模板名称定位。 |
| WEB-07 | P1 | `src/views/blacklist/BlacklistCenter.vue:8-11,107-115` | 新增黑名单按钮、解禁操作未使用 `v-permission`；普通角色菜单可能可见按钮，点击后才由后端拒绝。 | 页面仅依赖菜单可见性，未落实 D49 按钮级权限。 | 新增、解禁分别绑定 `BLACKLIST_ADD/BLACKLIST_RELEASE`；解禁仍由后端老板守卫兜底。 |
| WEB-08 | P1 | `src/views/blacklist/BlacklistCenter.vue:25,120` | 表格行键和解禁请求仍使用物理 `id`。虽然未展示，但与业务编码规范不一致，且接口迁移风险高。 | 黑名单接口仍是历史 Long ID 契约。 | 设计 `blacklistCode` 业务编码迁移；迁移完成前禁止机械替换，新增兼容层和条件更新测试。 |
| WEB-09 | P1 | `src/views/approval/ApprovalCenter.vue:11-14,254-257` | 审批页签可见性依赖本地 `ACTION_PERMISSION`/角色；后端最新权限收口后，存量权限或缓存不一致时可能出现页签可见但请求 403。 | 前端权限状态与后端审批类型守卫没有统一能力探测。 | 后端统一返回可审批类型，前端仅按该集合渲染；请求失败保留当前页并显示错误，不跳转 403。 |
| WEB-10 | P1 | `src/views/approval/ApprovalCenter.vue:69-92` | 渠道线索审批表使用 `row.leadNo` 作为 row-key，并在 `openAudit` 中伪装成 `approvalNo`。 | 线索审批没有统一的内部动作键。 | 后端返回统一 `approvalActionKey`（仅内部），展示继续使用提交人、事项、状态、时间；不要把 leadNo 作为审批号语义。 |
| WEB-11 | P1 | `src/views/Workbench.vue:4` | 页面标题仍为“我的工作台”，而 D65 已统一为“工作台”。 | 标题文本遗漏在历史改名范围外。 | 改为“工作台”，并同步面包屑、路由 meta 和可访问名称。 |
| WEB-12 | P1 | `src/views/Workbench.vue:368-378` | 最近匹配映射保留 `t.id` 作为前端行键；审计详情以 trace 传参虽合规，但未确认后端是否批量补齐客户名。 | 审计/匹配展示模型仍混用物理主键和 trace。 | 使用 traceUuid 或后端审计业务键作行键；主位展示客户姓名、结果、执行时间，trace 仅技术审计页可见。 |

## 二、P2 规范与性能问题

| 编号 | 优先级 | 路径与行号 | 问题 | 建议 |
|---|---|---|---|---|
| WEB-13 | P2 | `src/views/client/ClientProfile.vue:48-70` | 客户列表表格声明 `sortable`，但未绑定 `@sort-change`，建档时间只能当前页排序。 | 接入 `useTable.handleSortChange`，把 `orderBy/orderDir` 传后端。 |
| WEB-14 | P2 | `src/views/approval/ApprovalCenter.vue:75-91` | 渠道线索审批表有固定高度，但分配/渠道线索等分页查询仍依赖多套独立请求；统一审批接口已存在却未统一复用。 | 以统一待审接口作为聚合入口，或明确按页签懒加载并共享查询模型，避免重复请求。 |
| WEB-15 | P2 | `src/views/blacklist/BlacklistCenter.vue:25-60` | 列表未显式固定高度，数据少时无法填满容器；与 `loan-web-ui` 表格填充规则不一致。 | 统一 `height: calc(100vh - 320px); min-height: 360px`。 |
| WEB-16 | P2 | `src/views/rule/RuleTemplateList.vue:56` | 分类下拉使用 `c.id` 作为 key/value，属于物理 ID 暴露链路；后端已有分类业务编码能力但页面仍走 id。 | 增加 categoryCode 业务编码，名称作为 label，内部仅提交 categoryCode。 |
| WEB-17 | P2 | `src/views/approval/ApprovalCenter.vue:53-58` | 产品审批表“审核人”使用 `姓名待补充`，但未确认后端是否统一批量装配；名称缺失会影响 D63 审批人视角理解。 | 后端统一 `BusinessNameService` 批量装配申请人/审核人，缺失才显示统一占位文案。 |
| WEB-18 | P2 | 多页面 | 多处按钮仍直接写内联 SVG（如 `BlacklistCenter.vue:9`、`LeadPool.vue:9`），与 AppIcon registry 收敛目标不一致。 | 新增/改动页面统一使用 `AppIcon`；历史内联图标按页面批次迁移。 |
| WEB-19 | P2 | `src/views/plan/PlanEdit.vue:604`、`ChannelConfigWizard.vue:725` | 使用 `ElMessageBox.prompt` 作为业务输入弹窗，难以统一字段校验、无障碍和错误态。 | 迁移到 `AppDialog + el-form`；保留确认工具仅用于确认/提示，不承载复杂输入。 |
| WEB-20 | P2 | `src/layout/Layout.vue:372-421` | BOSS 报表菜单仍有前端临时补齐逻辑，后端菜单同步后若未移除可能造成双真源。 | 完成 `t_menu/t_role_permission` 同步后删除 BOSS 特判，并增加菜单路径零差异校验。 |

## 三、已核实的合规项

- `src/layout/Layout.vue:515-522` 已不再依赖错误的 `menus.find`，标题查找走 `findMenuItem` 和 `menus.value`，历史 `menus.find is not a function` 已在当前源码修复。
- `src/utils/request.js:63-74` 统一注入 `X-Client-Type: WEB`、token、trace 页面上下文；403 普通业务请求不会强制跳转 403。
- `src/components/AppTableActions.vue` 已提供操作列折叠与 nowrap；审批、客户、线索、工单多数页面已接入。
- `src/views/approval/ApprovalCenter.vue` 已按 D60 将渠道 PRODUCT/渠道线索终审限制为 `BOSS/SUPER_ADMIN/SUPER`，ALLOCATION 单独由后端团队范围校验。
- `src/views/client/ClientProfile.vue`、`src/views/report/*` 主表已优先展示姓名、企业名、顾问名，未发现主表直接展示客户/报告业务编码的普通列。

## 四、建议执行顺序

1. P0：先清理 D68 业务编码展示与表单输入（WEB-01～06），并补后端名称/业务键 DTO。
2. P1：补按钮级权限、审批类型能力探测、黑名单业务编码迁移设计（WEB-07～12）。
3. P2：统一排序、表格高度、分类业务编码、图标与复杂弹窗组件（WEB-13～19）。
4. 菜单双真源收口（WEB-20）后，做 7 角色菜单→路由→首屏 API→后端守卫矩阵回归。
