# 06 · 业务结论沉淀索引（C1–C22）

> **真源**：`artifacts/小程序模块结论沉淀.md`（顶部更新记录 + 模块小节）
> **纪律（用户明示）**：每次沟通涉及新结论/修正 → **先改真源** → 同步到落地清单/规格文档/交互原型/本知识库。

## 结论速查

| # | 模块 | 关键点 | 影响文件 |
|---|------|--------|---------|
| **09** | 业务流程知识图谱 | 角色/模块/接口/表/状态机 节点+边；JSON 机器可读；驱动流程图 | `09-业务流程知识图谱.md` + `artifacts/企融通业务流程图.html` |
| **C1** | 智能匹配操作资格 | 客户 + 全部 STAFF（除渠道）可操作；渠道是唯一不可操作角色 | `MiniMatchController.run` 守卫 |
| **C2** | 替客匹配 + 归属流转 | 新客户录入即归属；老客户有归属人自动归属；无归宿需审批 | `MiniClientService.create/claim` + 落地清单 |
| **C3** | 报告查询维度 | **角色二分**：客户仅按日期筛自己的；员工四维跨归属；渠道不可见 | `MiniMatchService.myReportsByDate / allReports` |
| **C4** | 报告命中产品可见性 | **仅员工可见**产品明细；对客脱敏（不返 productName/bankName） | `MiniMatchService.reportProducts`（**C19 接真实明细**） |
| **C5** | 经营诊断报告 | 五块：KPI/建议/风险/历年/多维；**复用匹配已上传材料**；独立详情页；**行业均值已表化（T1）：`t_industry_benchmark` + `IndustryBenchmarkService.avgByDimension`，缺失兜底硬编码 45/50/55/60/55** | `MiniMatchService.reportDiagnosis`（**C19 落地算法**） |
| **C6** | 经营诊断视觉重构 + 返回按钮排版 | 诊断详情页独立 + 排版修复 | `pages/report/detail.vue` 诊断 tab |
| **C7** | 服务单四维筛选 | 客户仅状态+时间；员工客户姓名/手机号/状态/时间；渠道隐藏 tab | `MiniOrderService.myOrdersByFilter / allOrders` |
| **C8** | 我的页账户字段 | 客户：实名/手机/性别/注册/邀请人；渠道：银行信息；员工差异化 | `pages/mine/mine.vue` |
| **C9** | 渠道产品删除 + 撤销审批 | 复用 `t_product_approval`（applyType=CREATE/DELETE）不新增表 | `MiniProductService` |
| **C10** | 录入新客户自动查重 | 输入即查（300ms 防抖），命中走归属流转，未命中录入 | `pages/match/match.vue` 步骤 0 + `MiniClientService.search` |
| **C11** | 报告新增客户姓名+统一社会信用代码 | 员工四维组合筛 | `MiniMatchService.allReports` 参数扩展 |
| **C12** | 全局排版 | roleBadge 竖排 / .subbar 标题竖排 / 上传按钮不铺满 | 设计系统 + 多个页面 |
| **C13** | 细节 UX 收尾 | 第 4 步隐藏底部按钮 + 服务单客户视图筛选条 + 录入按钮调整 + 渠道 tabBar 图标 | 落地清单 |
| **C14** | 全量 UI 审计 | 22 项缺陷（P0 7 / P1 8 / P2 7）；含 C4 零落地 / 运营超管无入口 / 删除审批流断裂 | `artifacts/loan-mini-原型UI审计与缺陷清单.md` |
| **C15** | 全链路落地 | A 原型 7 P0 + B 设计系统规范 + 前端 + 后端（`MiniMatchController/Service/MiniClientController/Service`） | 落地清单 §九-§十一 |
| **C16** | 第二轮收口 | P1 无障碍 8 项 + P2 打磨 7 项 + 4 页面（report/detail/order/list/mine/product）+ 后端 C7/C9 | 落地清单 §十二-§十五 |
| **C17** | **角色化导航** | 全端统一自绘 TabBar；store.role 修复 + AppIcon 扩 7 图标；移除原生 tabBar + switchTab → reLaunch | 落地清单 §二十 |
| **C18** | **Token 化收尾 + H5 hover** | home/index/auth 三页 100% Token 化；App.vue 全局 button hover + .u-hover 工具类；浅底 token 新增 | 落地清单 §二十一 |
| **C19** | **B 组数据模型缺口** | ① `t_screening_product` 明细表 + reportProducts 真实查询；② 诊断算法落地；③ `t_client_allocation_approval` 分配审批 + 3 接口；④ staffName 接 t_staff | `docs/plans/B组数据模型缺口实施方案.md` + 落地清单 §二十二 |
| **C20** | **渠道线索录入（T4）** | 渠道走 Lead 不走 Client（沙箱隔离）；归属=公海（`owner_staff_id=NULL`），由公司员工认领；`source` 字典修正为 BOSS/ADVISER/CHANNEL/VIP（旧 `MINI` 废弃）；AES+SHA 处理 `phone`/`credit_code`；唯一索引冲突返友好文案不泄归属人 | `MiniLeadController.submit` + `t_lead`/`t_lead_ent_ext` |
| **C21** | **OCR 回灌诊断（T2）** | 三层链路 `MiniUploadController → MiniMaterialService → SubmissionFactsMerger`（mergeFromOcr 仅补空）；`loan.ocr.provider=mock` 可插拔；`data_json._ocrMeta.version` 派生 `materialVersion`；诊断数据源复用智能匹配已上传材料 | `t_ocr_record` + `09-M_DIAG` |
| **C22** | **审批中心统一（T5）** | 结论「无统一审批表，采用视图层统一 + 入口统一」（方案 A），不建表、不迁移数据；白名单 `loan.mini.approval.types=ALLOCATION`；建表迁移列入阶段四 | `MiniApprovalController` + `04-审批中心` |

## 频繁被用户问到的问题与对应结论

| 用户问 | 看 |
|--------|------|
| 「切换用户登录选择客户类型无法根据用户角色切换菜单」 | C17 + 01-角色权限模型 |
| 「图标不显示」 | C17（C18 修复尝试）；CSS 空规则陷阱（02-红线） |
| 「工单 / 报告查询客户姓名手机号归属」 | C3 / C7 / C11 |
| 「匹配产品为什么看不到」 | C4（对客脱敏，员工看）；C19（明细查询） |
| 「客户录入了为什么没归属我」 | C2 / C10 |
| 「渠道能不能匹配/看报告」 | C1（C1 不可）/ C3（不可见） |
| 「员工能不能替客户匹配」 | C1（可，除渠道） |
| 「怎么加新图标」 | 05-前端工程要点 AppIcon 章节 |

## 待办 / 遗留

- **行业均值数据源**：~~C19 诊断 dimensions 用了预设常量；待接入真实行业数据~~ → **T1 已表化**：`t_industry_benchmark` + `IndustryBenchmarkService.avgByDimension`（见 C5 追加）
- **OCR 材料解析驱动诊断**：~~C19 仅用 submission.data_json~~ → **T2 已回灌**：`MiniUploadController → MiniMaterialService → SubmissionFactsMerger` 仅补空（见 C21）
- **统一审批中心迁移**：~~C19 分配审批独立建单~~ → **T5 已视图层统一**（方案 A，不建表）；建表迁移列阶段四（见 C22）
- **P2-6 320px 小屏走查**：T3 已完成响应式基线（`--fs-xxs:22rpx` / `u-shell` 限宽，见 05），信息密度类（22 处 22/23rpx 是否统一 24rpx）待设计拍板
- **上线配置真实值**（AppID / urlCheck / HTTPS 备案域名）待用户提供

## 跳到

- 接口契约：`04-后端 API 契约.md`
- 数据模型：`03-数据模型（DB schema 索引）.md`