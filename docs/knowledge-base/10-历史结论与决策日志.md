# 历史结论与决策日志（D 系列）

> **强制纪律**：任何修改/调整请求执行前，必须先读本文件，检查是否已有相关历史结论。
> 有结论 → 直接遵守，不得重复讨论或违背；无结论且不确定 → 停下来问用户。
> 新结论按**时间倒序**追加到表格最上方（最新在上）。

## 结论台账

| 编号 | 日期 | 场景/触发 | 结论（用户原意） | 影响文件/模块 | 状态 |
|------|------|-----------|------------------|---------------|------|
| D15 | 2026-08-31 | **规则 9 正式文本（含三类例外）· 唯一真源** | 规则 9「禁止写死易腐化的瞬时状态」的**唯一权威表述**；各文档/技能一律引用本条，**不得各自表述**。<br>**规则正文**：凡是会随代码或时间变化的值（表数、tabBar 数量、台账最大编号、非锚点 commit hash 等），一律写「**指向真源 + 取值命令**」，不写具体数值。例：写「以 `db/loan-db-schema.sql` 为准」，不写「68 表」；写「从最新条目往下读」+ `grep -o "^| D[0-9-]*" <台账> \| head -1`，不写「从 D14 往下读」。<br>**三类例外（必须保留原值，禁止抽象化）**：<br>① **恢复锚点** —— 如 D8 的 git 基线 `b80c965a`。反证：抽象成"最新 HEAD"后，删除文件再提新 commit 时该 HEAD 已不含被删文件，**回滚能力归零**；<br>② **纠错证据** —— 如 D13「`01` 文档写 3 tab 是错的、实测 4 tab」。抽象掉，"文档错了"即失去证据支撑；<br>③ **已执行动作记录** —— 如 artifacts 归档 9 个文件、D10 重号修复，是对"做过什么"的历史留痕。<br>**判据**：改这个值，读者是「**取到更新的值**」（→ 抽象）还是「**永远丢失一个不可逆事实**」（→ 保留）？拿不准就问用户，不得自行判断。<br>**事故记录**：2026-08-31 曾按规则 9 做清理时误删 D8 恢复锚点与 D12 行号记录，均已修复（D8 由 team-lead 写回、加注禁止抽象化原因；D12 由执行工程师自查后自纠）。<br>**根因**：当时技能文件里写的是无例外版本「（禁止在任何文档中写死该数值）」，导致错误被固化成规范并扩散至 5 处——**规则表述必须自带例外，否则照做即出错**。 | 所有文档、`docs/skills/**`、`.workbuddy/skills/**`、用户级技能 | 生效 |
| D14 | 2026-08-31 | **D10 语义澄清（"清理"的唯一定）** | **D10 中"纳入清理"一词存在歧义，本条为其唯一解释：「清理」= 归档（移动 + 批量改引用），不是删除。** 适用范围：`docs/` 根目录 5 份游离文档。<br>① `方案评审定稿纪要.html` 移入归档目录后**必须批量改指其 25 处引用**（分布于 12 个文件，其中 4 个 SKILL.md 视其为"唯一有效契约基线"），确保零死链、保住 D6 已改的 68 表成果；<br>② 其余 4 份（`channel-admission-v2-design.md`、`小程序H5落地清单-2026-08-30.md`、`ui-*.md`×3）一并归档；<br>③ `业务ID规范.md` 与技能 `loan-biz-id` 的**重复真源必须消重**（其被引用 4 处）。<br>**理由**：直接删除会一次性制造 25 条新死链（比修复前的 7 条更糟），且直接违反 T05 验收标准「死链清零」。 | `docs/方案评审定稿纪要.html`、`docs/业务ID规范.md`、4 个 SKILL.md、`docs/plans/` 4 份、`docs/knowledge-base/README.md` | 生效 |
| D13 | 2026-08-31 | 渠道 tabBar 数量（文档纠错） | `01-角色权限模型.md` 写"渠道 3 tab"，与代码不符。以 `loan-mini/components/TabBar.vue` 为准，渠道为 **4 tab**（首页·我的产品·录入客户·我的），01 文档待修正。 | `01-角色权限模型.md`、`.workbuddy/skills/role-channel/SKILL.md` | 生效 |
| D12 | 2026-08-31 | artifacts 归档缺口（死链修复） | README 与 06 引用的 `artifacts/` 5 个文件在仓库中不存在，实际位于外部会话目录 `/Users/admin/WorkBuddy/2026-08-25-14-58-50/.workbuddy/artifacts/`。**决策：cp 归档进 `docs/knowledge-base/artifacts/`（扁平、保留原文件名），不保留绝对路径**；png 截图与 V2 过期版本不归档。已执行 9 个文件（cp 非 mv，外部目录保持完好），README 5 条 artifacts 引用 **5/5 解析成功**，`小程序H5落地清单` **:26/:44** 两处引用已改指新路径（**已执行动作记录，按规则 9 例外条款保留原值**；行号会随后续编辑漂移，重新定位请用 `grep -rn "artifacts/" docs/`）。<br>【编号说明】本条初写为 D10，与同期并发写入的「清理范围扩大（E2）」重号，按"只改自己条目"原则改编号为 D12。 | `docs/knowledge-base/artifacts/`、`README.md:41,50-53`、`06` 第 09/C14 行、`docs/小程序H5落地清单`:26,44 | 生效 |
| D11 | 2026-08-31 | 知识治理体系设计（索引与技能架构） | 文档索引从编号制改为"按业务域"的索引层（G1-G9，编号文件 00-09 保留不动）；技能迁至项目级 `.workbuddy/skills/`（21 个）+ 用户级 `~/.workbuddy/skills/`（3 个）；业务 7 角色 + 团队 4 角色两套技能；角色技能一律以代码为准（如 ALLOCATION 审批不含 DEPT_MANAGER）。设计文档：`docs/plans/知识治理体系设计.md` | `docs/knowledge-base/README.md`、`11-业务域文档索引.md`、`index/*.json`、`.workbuddy/skills/**`、`docs/skills/**`（迁移后删） | 生效 |
| D10 | 2026-08-31 | 清理范围扩大（E2） | `docs/` 根目录 5 份游离文档**本轮一并纳入清理**（用户拍板，非架构师原建议的"下一轮"）：`方案评审定稿纪要.html`、`channel-admission-v2-design.md`、`小程序H5落地清单-2026-08-30.md`、`业务ID规范.md`、`ui-*.md`×3。其中 `业务ID规范.md` 与新建技能 `loan-biz-id` 疑为**重复真源**，必须消重——避免刚建完体系就留下双真源隐患。 | `docs/` 根目录 5 份、`docs/skills/loan-biz-id/`、`docs/knowledge-base/02-业务红线*` | 生效 |
| D9 | 2026-08-31 | 表数缺口处置（E8） | `docs/方案评审定稿纪要.html` 第 16 章域级明细加总仅 65，与真实 68 张**差 3 张未定位到业务域**。**不单开人工核对轮次**，交由一致性校验脚本新增检查项自动输出「schema 有而纪要未提及」的表名清单（该项标记 `WARN`，**不计入退出码**，否则脚本将永远返回 1）。索引统一写「以 `db/loan-db-schema.sql` 为准（当前 68 张 `t_` 表）」，不引 65。 | `db/loan-db-schema.sql`、`docs/方案评审定稿纪要.html` 第16章、`scripts/check-kb-consistency.py` | 生效 |
| D8 | 2026-08-31 | 删除安全基线 | 物理删除前必须先把 docs/ 全量 `git commit` 建立可恢复快照；`docs/plans/` 此前未纳入 git，删除不可恢复。已执行基线提交 **`b80c965a4837dfcf2fe2249728963980f8a0c54c`**（**恢复锚点，按规则 9 例外条款保留原值、禁止抽象化**：若写成「最新 HEAD」，则删除文件后又提新 commit 时，该 HEAD 已不含被删文件，回滚能力归零）：该次提交含 36 个 docs/ 文件，仅含 docs/ 路径，未混入无关工程改动。 | `docs/` 全量、`docs/plans/`、git 历史 | 生效 |
| D7 | 2026-08-31 | 蓝图文档不存在 | `前端交互逻辑蓝图.html`、`后端/前端逻辑蓝图.html` 经全仓搜索确认**不存在**（从未落盘），禁止再引用。契约真源改为三项：`db/loan-db-schema.sql`（表数以该文件为准）+ `loan-service` 实际代码 + `docs/knowledge-base/`。 | `docs/skills/knowledge-base-retrieval/SKILL.md`、`docs/skills/frontend-development/SKILL.md` | 生效 |
| D6 | 2026-08-31 | 契约基线以现实为准 | `docs/方案评审定稿纪要.html` 中的「65 表」**直接改为 68 表**（用户拍板不加勘误、不保留历史数字）。全库表数以 `db/loan-db-schema.sql` 为准（**本条不锁定具体数值**，避免随 schema 演进腐化；取值命令见 D0-2）。 | `docs/方案评审定稿纪要.html`（:73 / :403 / :522 共 3 处） | 生效 |
| D5 | 2026-08-31 | 知识治理总体要求 | 用户对本项目的知识治理提出 7 条要求：<br>① 整理 docs 和 skills 下最新文档、删除错误逻辑与描述；<br>② 每次把用户对助手说的结论记录下来；<br>③ 形成 skills，每次用户要求修改或调整时都要先检查历史结论；<br>④ 按模块业务逻辑建立文档索引；<br>⑤ 遇到不清楚的必须与用户讨论，不得臆断；<br>⑥ 按系统角色生成各自技能 skills；<br>⑦ 每次用户指出问题时自动更新。 | `docs/` 全量文档、`docs/skills/`、`docs/knowledge-base/`、本文件 | 生效 |
| D4 | 2026-08-31 | 结论日志位置 | 新建 `docs/knowledge-base/10-历史结论与决策日志.md`，按时间倒序追加 D 系列，作为「修改前必查」的唯一决策台账。 | `docs/knowledge-base/10-历史结论与决策日志.md` | 生效 |
| D3 | 2026-08-31 | 纠错方式 | 发现错误逻辑与过期描述 → **直接修正 + 物理删除**（非仅标记）。删除前列出具体路径清单。 | `docs/`、`docs/skills/` 下所有含失效路径/过期描述的文档 | 生效 |
| D2 | 2026-08-31 | 角色体系 | 技能要按**两套角色**都建——业务 7 角色（客户 / 渠道 / 顾问 / 部门经理 / 老板 / 运营 / 超管）+ 团队 4 角色（产品经理 / 架构师 / 工程师 / QA）。 | `.workbuddy/skills/`、`~/.workbuddy/skills/`、`docs/knowledge-base/08-*` | 生效 |
| D1 | 2026-08-31 | 技能存放位置 | **两处都要**：项目级 `loan-main/.workbuddy/skills/`（本项目专属、随 git）+ 用户级 `~/.workbuddy/skills/`（通用工作流习惯）。原因：现有 `docs/skills/` 不在系统加载路径，技能无法被自动加载。 | `.workbuddy/skills/`、`~/.workbuddy/skills/`、`docs/skills/` | 生效 |
| D0-4 | 2026-08-30 | 审批权限真值（来源：代码核查沉淀） | ALLOCATION 分配审批仅 `OPERATOR` / `SUPER_ADMIN` / `SUPER` / `BOSS` 可审（**不含 DEPT_MANAGER**）；PRODUCT / DOWNLOAD 审批才含 `DEPT_MANAGER`。依据代码 `MiniRoleGuard`（`APPROVER_ROLES` vs `APPROVAL_ROLES`）。**本条推翻**了《遗留项增量设计-2026-08-30》中的相反描述。 | `MiniRoleGuard`、`MiniApprovalController`、知识库 08 角色矩阵 | 生效 |
| D0-3 | 2026-08-30 | OCR 方法真名（来源：代码核查沉淀） | OCR 回灌方法真名为 `SubmissionFactsMerger.mergeFromOcr(reportNo, ocrFacts, operator)`，**不是** `mergeToOcr`。 | `SubmissionFactsMerger`、`MiniMaterialService`、知识库 03 / 06 | 生效 |
| D0-2 | 2026-08-30 | schema 表数（来源：`db/loan-db-schema.sql` 实测） | **记录时点（2026-08-31）实测为 68 张** `t_` 表；`t_industry_benchmark` 原已存在于主 schema（非本次新增）。表数以 `grep -c "CREATE TABLE" db/loan-db-schema.sql` 为准。 | `db/loan-db-schema.sql`、`docs/knowledge-base/README.md`、`docs/plans/` | 生效 |
| D0-1 | 长期 | 分工红线（来源：项目长期记忆） | 前端（loan-web / 小程序）由用户自己负责，助手只做后端 loan-service；定时任务必须用 XXL-Job，禁止 `@Scheduled`；业务 ID = 小写前缀 + 32 位随机。 | `loan-service/` 全部后端代码、`docs/knowledge-base/02-业务红线*` | 生效 |

## 状态说明

- **生效**：当前必须遵守
- **已被替代 → 指向新编号**：被后续结论推翻，保留供追溯
- **已废弃**：不再适用

## 推翻规则

结论被推翻时**不要删除原条目**，改为：状态列写「已被 Dxx 替代」，并在新条目中说明推翻了哪条。

## 追加模板

新增结论时复制到表格**最上方**（最新在上）：

```
| D<序号> | <YYYY-MM-DD> | <场景/触发> | <结论（用户原意，忠实记录）> | <影响文件/模块> | 生效 |
```
