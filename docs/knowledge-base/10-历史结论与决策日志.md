# 历史结论与决策日志（D 系列）

> **强制纪律**：任何修改/调整请求执行前，必须先读本文件，检查是否已有相关历史结论。
> 有结论 → 直接遵守，不得重复讨论或违背；无结论且不确定 → 停下来问用户。
> 新结论按**时间倒序**追加到表格最上方（最新在上）。
>
> **⚠️ 关于本文件内的旧路径（勿"修复"）**：本文件是历史决策台账，条文中的路径记录的是**作出该决策时的事实**。例如 D6/D9 记录时纪要位于 `docs/方案评审定稿纪要.html`，现已归档至 `docs/plans/archive/`；D7 记录时的 `docs/skills/` 也已迁移删除。按 **D15 例外②③**（纠错证据 / 已执行动作记录必须保留原值），这些原值**保留不改**。因此 `scripts/check-kb-consistency.py` 的当前表数与死链检查会排除本台账；不得为追求校验器全绿而修改历史数字或旧路径，否则将摧毁历史证据。

## 结论台账

| 编号 | 日期 | 场景/触发 | 结论（用户原意） | 影响文件/模块 | 状态 |
|------|------|-----------|------------------|---------------|------|
| D53 | 2026-09-03 | 按角色维度排查 Web 动态菜单、前端路由与权限的真实错配 | **权限链路真值**：每个菜单必须同时对齐“动态菜单 path → 前端路由/子路由继承 → 页面实际首屏 API → 网关角色鉴权 → Controller/数据范围守卫”，不允许只验菜单数量。**真实 Bug 1**：应用内把 `OPERATOR/SUPER_ADMIN/SUPER` 当管理全量角色，网关 `superRoles` 却只发布 `BOSS`，新增渠道线索审批接口导致 SUPER 两角色菜单/页签可见但网关 403；现统一为 `BOSS/OPERATOR/SUPER_ADMIN/SUPER` 业务默认全量，但 BOSS 系统配置显式拒绝规则优先。**真实 Bug 2**：`/403?from=...` 按 fullPath 被反复写入多标签，且空菜单角色切换时不清旧标签；现 `/403` 永不创建/持久化标签，角色菜单返回后统一按 `canAccessRoute` 清理，合法子路由按父菜单继承。**角色边界**：BOSS 只保留业务全量/审批，不可配置组织、菜单、接口权限；`OPERATOR/SUPER_ADMIN/SUPER` 是系统配置管理角色；渠道终审只有 `BOSS/SUPER_ADMIN/SUPER`，DM/OPERATOR 页面不请求该列表；CHANNEL 只走 `/api/channel/**` 本人数据。**真实回归**：JDK8 + Nacos prd，7 角色菜单数 BOSS24/DM20/ADVISER7/OPERATOR26/SUPER_ADMIN27/SUPER27/CHANNEL5；按实际页面首屏依赖共 231 次网关请求零失败，反向越权符合预期。测试脚本的菜单→API 映射必须先从页面源码取证，不得把测试脚本错配误判为业务 Bug 并扩大权限。 | `ApiPermissionService`/`ApiAuthGlobalFilter`、`routeAccess.js`/`Layout.vue`、权限单测、角色/网关 skills、知识库01/04/07 | 生效 |
| D52 | 2026-09-03 | 奖励规则列表报 `Unknown column 'product_code'` 后要求修改至无错 | 根因为当前代码已按“产品×客群”查询 `t_reward_rule.product_code/customer_group`，但 Nacos `prd` 指向的远程 `loan_db` 未执行对应迁移。已预检确认表中仅一条历史 `V1` 规则、无可去重组合，执行最小增量迁移：新增两字段及 `uk_product_group(product_code,customer_group)` 唯一索引，并将 `rule_version` 从 32 扩容到 64 以容纳 `BizIdGenerator` 生成值；历史规则维度保持 NULL，不猜测业务归属。迁移脚本改为基于 `information_schema` 的幂等实现，重复执行成功。原应用 SQL 在 prd 查询成功，当前 IDEA 旧进程经 8080 直连与 8088 网关真实调用 `GET /api/admin/reward/rule` 均返回 HTTP 200/code 0。同时将奖励规则更新/停用从物理 `Long id` 改为 `ruleVersion`，实体物理 ID 不再序列化给前端，Web 调用同步；写入时将唯一索引冲突转为统一业务异常，更新仅覆盖实际提交字段。附件中独立的 XXL-JOB 错误是 prd 仍为 `PLACEHOLDER_XXL_*` 却启动注册线程；`XxlJobConfig` 现在占位配置不创建执行器，注入真实配置后自动恢复。验证：JDK8 后端全量非 L3 测试 307 项（失败 0、错误 0、跳过 1），新增重复规则冲突测试后奖励目标测试 32 项、XXL 配置测试 2 项均全通过；Web 生产构建成功；`git diff --check` 通过。当前代码重启约 13 秒，新进程通过 8088 返回 HTTP 200/code 0，响应已不含物理 `id`；新启动日志无本次 SQL 和 XXL 注册错误。 | `db/migrate-reward-rule-dimension.sql`、`db/loan-db-schema.sql`、远程 `loan_db.t_reward_rule`、`RewardRule/RewardService/RewardController`、`XxlJobConfig`、Web 奖励规则 API/页面、知识库 04 | 生效 |
| D51 | 2026-09-03 | 权限变更 SQL 执行前读取 Nacos prd 并回读校验 | 按 Nacos `prd` 配置连接远程 MySQL `110.42.219.5:9306/loan_db`，未启动本地数据库/Nacos。`db/migrate-api-perm.sql` 2 条语句全部成功（幂等）；`db/menu-permission-seed-2026-09-01.sql` 12 条语句在事务中提交成功。回读结果：`t_menu` 28、`t_api_permission` 242、`t_role_api` 609；角色权限 ADVISER 7、BOSS 26、CHANNEL 5、DEPT_MANAGER 20、OPERATOR 26、SUPER 27、SUPER_ADMIN 27。CHANNEL 菜单仅 `/workbench`、`/lead`、`/client`、`/product`、`/report/screening`；示例管理员与渠道账号校验正常。未执行全量 schema/init-data，未修改业务表。Nacos 中密码仍为占位值，本次未将敏感值写入代码或文档。 | 远程 `loan_db`、权限 SQL、Nacos `application.properties@loan/prd` | 生效 |
| D50 | 2026-09-03 | 合作渠道管理员查看本人客户、分析报告、归属信息及渠道新增审批边界 | **渠道只读数据范围**：CHANNEL 可在 Web 查看本人录入并已转化的客户档案、这些客户的分析报告及归属顾问姓名；列表与详情均由后端按渠道 `userNo` 强制隔离，兼容历史 `ext_json.recorderChannelNo`，并以 `client_profile_code/lead_no/手机号 hash+客群` 作转化关联兜底；直接猜测其他渠道的 `clientCode/reportNo` 返回 FORBIDDEN。客户与报告同时支持单条、最多 100 条批量及组合分页查询，批量一次查库、去重保序且忽略越权/未命中项；对外剔除归属工号、个人档关联编码、匹配 trace 和模板编码。**禁止项不变**：不可发起匹配、不可查看命中产品明细、不可认领/指派/回收/编辑客户、不可查看其他渠道数据。**渠道线索**：CHANNEL 有权新增；Web/小程序新增强制 `source=CHANNEL`、录入主体=`userNo`、初始 `PENDING_APPROVAL`，新增后立即进入渠道本人“我的线索”，待审/通过/驳回均可见且其他渠道不可见；由 `BOSS/SUPER_ADMIN/SUPER` 任一单级终审，通过进入 `NEW` 公司公海，驳回 `REJECTED` 且不进入公海，条件更新防重复审批。**网关**：CHANNEL 保持 `channel:*` 前缀，并以 method+path 精确白名单开放自有产品和本人线索等必要 mini 接口，禁止整体开放 `mini:*`。**接口与菜单**：新增 `/api/channel/lead|client|report/**` 专用接口；CHANNEL Web 菜单由 3 项扩为 5 项（工作台、我的线索、我的客户、我的产品、客户分析报告）。本条覆盖 C2/C3 与 role-channel 中“渠道不可见客户/本人报告/归属”的旧限制，不开放小程序报告 tab。 | `ApiPermissionService`、`ApiAuthGlobalFilter`、`MiniLeadService`、`ChannelDataScopeService`、渠道 Controller、Lead 审批、Web/小程序渠道页面、role-channel、知识库 01/04/06/08/09 | 生效 |
| D49 | 2026-09-03 | Web 权限、审批、客户、公海、工单按钮级收口与后端重复名称装配提取 | **① Web 操作权限统一**：新增 `loan-web/src/utils/access.js` 作为操作权限与工单状态机唯一入口；审批审核/作废、客户认领/直分配/回收、线索认领/指派/删除、工单新建/状态流转均按权限码 + 状态判断，渠道隐藏公海与归属操作；顾问保留下载申请权限。**② D39 前后端一致**：部门经理显示 Web 分配审批页签，后端 `MiniRoleGuard` 允许其审批本人团队 ALLOCATION，跨团队仍由 BOSS 审批；产品/下载审批按实际类型传入守卫。**③ 名称装配复用**：新增 `BusinessNameService`，统一员工、客户、产品及邀请引荐人批量名称查询，审批/客户/归属/工单/邀请服务接入，单条解析复用批量入口，列表无行级 N+1；不再向邀请绑定结果暴露历史 `referrerId`。**④ 下载状态收口**：仅已通过且未作废的下载审批可作废。验证：Web Vitest 17/17、后端 JDK8 编译通过，目标回归 40/40；x86_64 JDK8 在 arm64 主机 fork 测试无法启动 `/bin/sh`，使用 `-DforkCount=0` 完成回归。**⑤ 技能/契约同步**：修正 D0-4 被 D39 覆盖后的审批权限描述。 | `loan-web/src/utils/access.js`、审批/客户/公海/工单页面、`loan-service/src/main/java/com/loan/common/service/BusinessNameService.java`、相关服务与测试、`.workbuddy/skills/*`、知识库审计基线 | 生效 |
| D48 | 2026-09-03 | 阶段 2 小程序列表状态与名称装配优化 | 小程序渠道客户录入列表统一使用 `AppSkeleton` / `AppEmpty` / `AppLoadMore`，错误态与空态互斥，加载和加载更多均防重复请求；产品列表首次加载骨架屏与空态互斥。客户轻量分页一次批量装配 `ownerStaffName`，避免逐行查询；邀请绑定、产品城市关系的幂等与并发条件更新测试继续作为回归基线。验证：小程序 Vitest 12/12、Web Vitest 12/12、H5 构建成功、后端目标测试与 JDK8 编译通过。 | `loan-mini/pages/client/create.vue`、`loan-mini/pages/product/list.vue`、`ClientService.pageLite`、知识库审计基线 | 生效 |
| D47 | 2026-09-03 | 阶段 1 计划编排接口与跨端 UI 审计整改 | **执行计划主资源统一使用业务编码**：详情、更新、删除、复制、模版应用、另存为模版均按 `planCode/templateCode` 定位；创建/复制/应用计划返回 `planCode`，更新请求不允许改写业务编码。计划模块/步骤仍保留物理 FK，仅作为计划树内部节点。策略模版 Controller 路径变量显式绑定 `templateCode`，避免参数名丢失。小程序锁图标统一使用 `AppIcon`，清理 emoji 专用残留样式；Web 计划/模版/策略列表行键改用业务编码。验证：JDK8 后端编译通过，Web Vite 生产构建通过（仅既有 chunk/router warning）。 | `AdmissionExecutionPlanController`、`PlanOrchestrationService`、`StrategyTemplateController/Service`、Web 计划编排 API/页面、小程序首页/认证/匹配/我的页面 | 生效 |
| D46 | 2026-09-02 | 阶段 0 审计与首批缓存实现 | **阶段 0 基线落盘**：新增《跨端代码与交互优化审计基线》，记录重复查询、`selectById`、N+1、缓存候选、Web/小程序角色入口和 TODO 分级。新增 `UnifiedCacheService`，对角色菜单、部门树、角色列表提供 Caffeine 一级 + Redis 二级缓存，按 key single-flight、Redis 故障降级、TTL 抖动，写后显式双层失效；客户归属、审批状态、会话、验证码和敏感资料禁止进入通用缓存。验证 `UnifiedCacheServiceTest` 4/4 通过，`mvn -q -pl loan-service -am -DskipTests compile` 通过。后续接口迁移、前端改造和真实联调按 D45 阶段顺序执行。 | `loan-service/src/main/java/com/loan/common/cache/UnifiedCacheService.java`、`OrgService`、`OrgWriteService`、`docs/knowledge-base/12-跨端代码与交互优化审计基线-2026-09-02.md` | 生效 |
| D42 | 2026-09-02 | 用户明确本项目本地 JDK 位置为 `/Users/admin/Documents/developer/jdk1.8` | **JDK 执行路径校正**：该路径是 macOS JDK 包根，实际 `JAVA_HOME=/Users/admin/Documents/developer/jdk1.8/Contents/Home`，版本为 JDK 8；`scripts/service.sh` 后端与网关直接执行该目录下 `bin/java`，`run-dev-prd.sh` 将其列为首选并实际验证版本。其余 D41 Nacos `prd` 配置源与 D37 五项参数不变。 | `scripts/service.sh`、`scripts/run-dev-prd.sh`、`loan-service-ops` | 生效 |
| D43 | 2026-09-02 | 本轮集中回归：后端全量测试、Web/小程序构建与页面展示审计 | **集中修改后统一验证**：先完成后端会话类型隔离、员工手机号 AES 单写、产品 DTO/城市批量映射、审批类型安全默认（空配置仅开放 ALLOCATION）、Web 客户档案空状态和产品名称主显，再执行一次后端 275 项测试、Web 12 项测试及生产构建、小程序 12 项测试与 H5/mp-weixin 构建；全部通过。发现并记录：Maven/JUnit 测试日志中的预期异常堆栈不等于失败；Vite 大 chunk 与 uni-app vue-router 弃用提示属于性能/依赖优化项，不阻断联调；远程 XXL-JOB 占位 IP/token 仍需上线前配置。 | `loan-service`、`loan-web`、`loan-mini`、`docs/knowledge-base/artifacts/Web与小程序当前代码审计及视觉重构路线图-2026-09-02.md` | 生效 |
| D44 | 2026-09-02 | 单次重启后的真实角色矩阵与启动耗时记录 | **真实回归结果**：后端使用原生 arm64 JDK 8、Nacos `prd` 五项参数启动，健康接口最终 200；`/api/auth/me`、菜单树、客户轻量分页、产品分页、审批统一待审/计数按 BOSS/DM/ADVISER/CHANNEL/CUSTOMER 验证。员工三角色均可访问业务接口；CHANNEL 仅可访问本行产品分页，客户/渠道访问管理端其余接口统一 2001；空配置审批类型 PRODUCT 返回空结果，ALLOCATION 正常。注意客户列表真实接口为 `GET /api/admin/client/page-lite`，不要误测不存在的 `/page`。本次冷启动约 **476 秒**；主要耗时仍在 AspectJ 首次织入、远程数据库 `SELECT 'x'`（约 15 秒）及 Nacos/Bean 初始化，XXL-JOB 占位 IP/token 注册失败为非阻断配置告警。 | `scripts/service.sh`、`/tmp/loan-service-dev.log`、`loan-service` | 生效 |
| D45 | 2026-09-02 | 用户要求系统化优化前后端与小程序代码、缓存、权限及 UI/UX，并强调分阶段、集中测试、吸收启动坑点 | **执行计划冻结**：按阶段 0～5 推进，先审计和契约矩阵，再做后端查询/权限/稳定性，随后按证据接入 Caffeine+Redis 缓存、日志和启动优化，之后重构 Web 与 H5/小程序角色化菜单/审批/工单交互，最后只重启一次做全角色真实回归。未确认的缓存一致性、删除名单、数据库变更和业务口径列为用户确认闸门；不修改上线 AppID/HTTPS 配置，不切换 JDK8 + Nacos `prd` 约束。 | `docs/plans/前后端小程序代码与交互优化计划-2026-09-02.md`、相关 skills 与知识库 | 生效 |
| D41 | 2026-09-02 | 用户再次明确本地真实数据联调不需要本地 Nacos 或本地数据库，要求直接读取 Nacos `prd` 的数据库等环境配置并长期记住 | **本地联调配置源固定**：本地只运行当前代码进程；`loan-service` 必须使用 JDK 8，并携带 D37 约定的 5 项 `-D` 参数连接 `124.221.150.239:9848`、`namespace=prd`。数据库、Redis 及其他已在 Nacos 管理的后端基础设施配置一律读取 `prd` 下发值，禁止新增本地数据源覆盖、禁止改用 `dev` namespace、禁止因联调另起本地 Nacos/MySQL/Redis。敏感配置只验证是否成功下发，不写入代码、启动脚本或测试文档。Web/小程序仍通过本地 8088 网关访问本地 8080 当前代码。 | `scripts/run-dev-prd.sh`、`scripts/service.sh`、`loan-service` Nacos 引导配置、`07-沟通-上线-测试-部署清单.md`、`loan-service-ops` | 生效 |
| D40 | 2026-09-02 | 用户要求继续形成 Web + 小程序完整问题清单，并将视觉重构、图标统一和四阶段路线图统一落入知识文档 | **当前审计入口统一**：以 `artifacts/Web与小程序当前代码审计及视觉重构路线图-2026-09-02.md` 作为现行源码整改入口；问题必须记录路径、审计时行号、表现、根因和修复方向，并区分“仍存在/已修复待回归/历史问题”。**业务边界不变**：首页不恢复手填邀请码；分享链接/二维码自动携带引荐码；顾问服务暂放【我的】；引荐人与服务顾问分离；姓名/名称主显；客户分配与转移继续执行 D39。**实施顺序**：先正确性和权限，再名称主显与规模化选择器，再视觉/组件/图标收敛，最后做全角色、真机、弱网验收；真实 AppID、HTTPS 等上线配置继续按 D33 最后处理。 | `artifacts/Web与小程序当前代码审计及视觉重构路线图-2026-09-02.md`、知识库 README/11、Web/小程序后续整改 | 生效 |
| D39 | 2026-09-01 | 用户要求「管理端指定顾问改为管理员直接落归属、无需审批」，并明确团队管理者/老板可直接分配、DM 仅审批本团队、跨团队向上 BOSS、顾问替客匹配对他人归属客户须审批、并完善回收与预警（参考 tse-frontend 分配/回收/预警范式） | **① 直接指定归属（无需审批）**：`POST /api/admin/client/{clientCode}/assign` 由 BOSS/OPERATOR/SUPER_ADMIN/SUPER/DEPT_MANAGER 直接落归属（覆盖式 `assignOwnerIfUnchanged`，可重分配已归属客户），**不生成审批单**；目标=ADVISER/DEPT_MANAGER（`ASSIGNABLE_ROLES`）；前端操作前须弹窗二次确认。<br>**② DM 审批范围 + 向上 BOSS**：`MiniRoleGuard.APPROVER_ROLES` 纳入 DEPT_MANAGER；`pendingPage` 仅返回本人团队（申请人部门==DM 部门）待审单；`approve`/`reject` 调 `assertDeptManagerScope`，跨团队单抛 FORBIDDEN「非本团队客户，需由 BOSS 审批」。<br>**③ 替客匹配归属强制审批（P0-3）**：`MiniMatchService.runForMini` 守卫——目标客户已归属他人且非本人且无已通过归属审批（`hasApprovedOwnership`）→ FORBIDDEN 引导至「客户分配」发起审批；`MiniClientService.claim`→`applyTransfer` 生成 ADVISER_TRANSFER 待审单经统一审批流转。<br>**④ 客户回收+预警（P1）**：新增 `t_client_recycle_config`（全局单行：回收开关/天数/预警天数/冷却天数）；`recycleOverdue`/`warnRecycle` 由 XXL-Job `clientRecycleJob`/`clientRecycleWarnJob` 调度；管理端 `POST /api/admin/client/{clientCode}/recycle` 手动回收（覆盖冷却）；回收仅 `LambdaUpdateWrapper` 更新非加密字段规避 AES 二次加密；`lastFollowedAt`/`assignBlockedUntil` 落 `t_client_profile`，归属/审批通过时 `touchAssignment` 刷新（⚠️ 跟进模块未落地，当前基线=归属/转移，见 06-C26）。<br>**⚠️ 推翻 D38 部分条款 + D0-4 ALLOCATION 条款**：原「DEPT_MANAGER 可发起指定但不可审批，审批仍仅限 BOSS/OPERATOR/SUPER_ADMIN/SUPER」作废——DM 现为本团队 ALLOCATION 审批人。 | ClientController/ClientAllocationService/MiniRoleGuard/MiniMatchService/MiniClientService/MiniMatchController/ClientRecycleJobHandler/ClientRecycleConfig/ClientProfile/Notification + 知识库 01/03/04/06 | 生效 |
| D38 | 2026-09-01 | 用户校正小程序分享引荐、服务顾问与未分配客户归属规则，并要求补齐管理端闭环 | **关系解耦**：分享小程序链接/二维码自动携带邀请码，有效期内自动绑定引荐链；引荐人不等于服务顾问，绑定不得回写 `ownerStaffCode`，不得伪造 `INVITE` 线索。<br>**客户归属**：新微信客户 `ownerStaffCode=NULL` 即进入未分配客户池；顾问可申请认领，管理角色可按客户业务编码选择有效顾问并发起分配，两入口统一创建 `ALLOCATION` 待审单。`DEPT_MANAGER` 可发起指定但不可审批，审批仍仅限 `BOSS/OPERATOR/SUPER_ADMIN/SUPER`。同客户待审唯一、重复请求幂等、审批状态和最终归属均用条件更新防并发覆盖。<br>**展示规则**：小程序顾问服务暂放「我的」；`/api/mini/me` 分开返回 `ownerStaffName/referrerName`；小程序、Web 主位只显示客户/顾问/银行/部门名称，复杂业务编码仅作接口参数和内部行键。 | `ClientAllocationService`、`InvitationService`、客户/审批接口、Web 未分配客户页签、小程序「我的」/服务单/审批列表、DB 迁移、知识库 01/03/04/05/08/09 | 已被 D39 部分替代（DM 审批条款） |
| D37 | 2026-09-01 | 用户要求修复本地后端启动工作目录与日志绝对路径，并指定后端必须使用给定 5 项 `-D` 参数启动，其余服务不变 | **本地后端启动契约**：`scripts/service.sh start/restart backend` 先切换到 `/tmp/loan-runtime` 再执行 Java；Log4j2 通过 `LOAN_LOG_DIR=/tmp/loan-runtime/logs` 使用绝对日志目录，不再附加 `-Duser.dir`。Java 仅使用 `-Dnacos.server-addr=124.221.150.239:9848`、`-Dnacos.namespace=prd`、`-Dspring.cloud.nacos.discovery.register-enabled=false`、`-Ddubbo.enabled=false`、`-Dapp.gateway.trust-only=false` 五项系统参数。网关、Web、小程序启动方式保持不变。 | `scripts/service.sh`、`loan-service/src/main/resources/log4j2-spring.xml` | 生效 |
| D36 | 2026-09-01 | 用户确认下一批业务 ID 改造，指定渠道名单/产品城市关系前缀并要求总长度控制为 16；随后明确查询需支持批量，修改按业务唯一编码或主键唯一 ID 定位 | **编码契约**：`t_channel_user_list.list_code` 使用 `culist` + 10 位小写字母数字，`t_bank_product_city.product_city_code` 使用 `pcity` + 11 位小写字母数字，总长度均固定 16；这是仅限这两张关系记录表的用户确认例外，其余记录级业务 ID 继续遵守“前缀 + 32 位随机”。<br>**数据库已执行**：远程预检两表均 0 行、自然键无重复、目标列/索引不存在；执行 `migrate-relation-bizcode-16.sql` 后，两列均为 `varchar(16) NOT NULL`，`uk_list_code`/`uk_product_city_code` 均为唯一索引，异常编码数 0。<br>**全链路改造**：后端查询支持单条、批量、组合分页/产品列表；详情、修改、删除统一按 `listCode`/`productCityCode` 业务唯一编码定位，修改体不接收物理 `id` 且不允许改写业务编码，实体响应隐藏物理 `id`；批量新增从循环逐条查库/写库改为内存去重 + 单次 `INSERT IGNORE`，由业务编码和自然业务键唯一索引保证并发幂等；Web API、行键、单删、批删和城市解绑同步使用业务编码。 | `db/loan-db-schema.sql`、`db/migrate-relation-bizcode-16.sql`、`ChannelUserList*`、`BankProductCity*`、Web 渠道名单/产品城市、`loan-biz-id`、`loan-code-standard`、知识库 03/04/10 | 生效 |
| D35 | 2026-09-01 | 用户明确前后端代码标准化约束，并要求沉淀为项目 skills 后持续执行 | **职责与复用**：前后端按业务/技术边界清晰分层，优先复用已有能力；语义一致的重复类、方法、查询构造、utils 与组件必须评估提取，变化点使用策略/Handler/配置扩展，禁止万能工具类和复制整页。<br>**后端接口与稳定性**：同一资源支持单条、批量、组合条件分页并复用统一 Query 模型，禁止一个条件一个方法与 N+1；按事实选择索引、Caffeine/Redis、幂等键、唯一索引、CAS、锁及线程安全方案；业务异常统一由全局处理返回标准结果；高风险/高成本接口具有限流限频，外部依赖具备超时、隔离、熔断和安全重试；traceId 从网关贯穿 HTTP/RPC/异步任务；业务/错误日志按日期与大小滚动、biz/error 分流、中文结构化记录并脱敏。<br>**前端展示**：用户、企业、产品、部门等主位展示姓名/名称/描述，复杂 ID 或业务编码不作唯一可见信息；确需排查时降为次要信息，名称由后端批量补齐，禁止逐行 N+1。<br>**唯一真源**：标准落在项目实际加载路径 `.workbuddy/skills/loan-code-standard/`，现有端与团队 skills 只引用和设置门禁，不在已废弃的 `docs/skills/` 建重复副本。后续新开发、修改、重构、评审和 QA 均执行此标准；具体性能措施须基于调用量、一致性和失败模式，不能无差别叠加。 | `.workbuddy/skills/loan-code-standard/**`、`loan-backend`、`loan-web-dev`、`loan-mini-ui`、`loan-gateway-auth`、`team-engineer`、`team-qa`、知识库 02/README | 生效 |
| D34 | 2026-09-01 | 测试默认策略与存量管理接口业务 ID 成熟度改造 | **测试策略**：Maven 从默认跳过改为默认执行测试，联调启动/快速打包脚本仍显式 `-DskipTests`；后端全量非 L3 测试通过。Web 增加菜单数组容错回归，mini 修复条件编译常量与 Vitest 冲突，Web/mini 测试与构建均通过；页面/业务流测试仍需逐步加深。<br>**业务 ID 第一波**：渠道准入策略管理契约从自增 `Long id` 改为已有唯一 `strategyCode`；跨渠道复制改用 `sourceStrategyCode`，模版导入改用 `templateCode`，创建/复制/导入返回业务编码。后端、Web API/页面/向导已同步，未改表。<br>**待确认边界**：计划/模版的模块与步骤、渠道名单、产品城市关系只有物理主键，彻底迁移需新增业务编码列/唯一索引并回填存量数据，实施前需用户确认数据库方案。 | `pom.xml`、`loan-mini/api/request.js`、Web 菜单回归测试、`ProductStrategyController/Service`、Web 渠道策略 API/页面、知识库 04/07/10 | 生效 |
| D33 | 2026-09-01 | 用户要求先调通 App、Web、网关、后端管理联动，再修正文档偏差；真实上线配置最后处理 | **① 四端联调口径**：App 按现有 `loan-mini` 的 H5 / 微信小程序实现；本地四端为 backend:8080、gateway:8088、Web:5173、mini H5:5174。Web 与 mini 开发请求统一经 8088 网关，禁止默认直连 8080 绕过鉴权。<br>**② 保活实测**：当前 Codex 执行环境中 `nohup + disown` 会随执行会话回收，不能作为可靠保活；`scripts/service.sh` 改为 macOS 用户级 `launchctl submit`。受 macOS 后台访问 Downloads 限制，backend / gateway 先构建 JAR，再复制到 `/tmp` 运行；backend / gateway / Web 日志写 `/tmp`，mini 写项目日志。watchdog 也改由用户级服务托管。<br>**③ 联动证据**：四个 `com.loan.dev.*` 服务标签均在运行；8080 / 8088 / 5173 / 5174 状态全绿，网关公钥、Web、mini HTTP 均为 200；BOSS 经网关登录并读取菜单、工作台、产品、审批、报表成功；mini mock 登录经 5174→8088→8080 成功，客户访问渠道产品被正确拒绝。后端编译、Web build、mini H5 build、mp-weixin build 均通过。<br>**④ 文档与上线顺序**：README / 结构 / 部署 / 运维 skill 按当前代码修正；表数、页面数等动态值改为真源 + 查询命令。真实 AppID、HTTPS 域名、`urlCheck` 等上线配置按用户要求最后处理，本轮只记录开发现状。 | `scripts/service.sh`、`loan-mini/vite.config.js`、`loan-mini/api/request.js`、`README.md`、知识库 00/03/07/10/README、`loan-service-ops` | 生效 |
| D32 | 2026-09-01 | 用户反馈 Web 全页无法操作，控制台报 `menus.find is not a function` | **根因与修复**：`Layout.vue` 中 `menus` 是 Vue `computed`，但面包屑标题和多标签标题两处把 computed ref 当普通数组直接调用 `.find()`，在挂载阶段抛错导致布局无法操作。两处统一改为读取 `menus.value`，并增加数组、空 path 防御。**验证**：Web 现有 9 项单测通过、生产构建通过；真实页面刷新后依次点击客户档案/审批中心/产品库成功，多标签新增、切换、关闭均正常，浏览器控制台无 error。旧页面若仍显示旧 chunk 堆栈，需强制刷新加载新模块。 | `loan-web/src/layout/Layout.vue`、`scripts/service.sh` | 生效 |
| D31 | 2026-09-01 | 用户反馈"菜单点击无反应 + 非角色权限菜单不必显示"，要求按 D30 落地 apiperm 本地校验 + 数据补齐 + 端到端验证 | **① 菜单重复归因**：截图显示 BOSS 端"匹配与规则/服务与审批/服务工单/审批中心"重复——核查后确认**数据/代码零重复**（Layout 27 path 唯一、t_menu 28 path 唯一、App.vue 单 router-view、模板单 v-for），归因 **Vite HMR 多次热重载导致的侧栏 DOM 残留（浏览器缓存）**，Ctrl+Shift+R 强刷即可清除，非代码 bug。<br>**② apiperm 本地校验接入（D30 方案落地）**：`ApiPermissionService` 增 `check(user, method, path)`（@PostConstruct 构建 pathPattern→apiKey 缓存 + Spring AntPathMatcher 匹配）；`AdminAuthInterceptor` 码级校验后接入（第三层）。校验逻辑：非 /api/admin 放行 → BOSS superRole 放行 → CUSTOMER/CHANNEL 放行（白名单/typeRules 控制）→ STAFF 管理角色（OPERATOR/SUPER_ADMIN/SUPER）默认全量 → DM/ADVISER 查 t_role_api（**命中放行、未命中保守放行**，D30 方案防误伤；未来切严格仅需改一行 + 补全授权）。<br>**③ 数据补齐**：远程库 `INSERT IGNORE` 为 OPERATOR/SUPER_ADMIN/SUPER 各补 152 条 admin:* 接口授权（3×152=456 行，来自 t_api_permission 中 api_key LIKE 'admin:%' 或 path_pattern LIKE '/api/admin/%'）；DM 74 + ADVISER 49 保留（接口清单同步自动生成口径）；CHANNEL 不配（白名单生效）。<br>**④ 端到端验证（稳定后连测通过）**：BOSS org/staff=0（superRole）、OPERATOR/SUPER_ADMIN/SUPER org/staff=0（管理角色全量）、DM org/staff=2001（码级 page:org 先拦）、ADVISER 线索/工单/菜单树=0（现有工作流不受 apiperm 影响）、DM audit=0（工作台复用）、CHANNEL 产品分页=0（白名单）、SUPER 调 debug=2001（T17 开关）；菜单树 BOSS 26/DM 20/ADVISER 7/CHANNEL 3；sms/template-page=404（路径不存在非权限）。⚠️ 教训：后端重启后首次测试结果波动（2001/菜单数错乱/500）系后端预热+Redis 会话瞬时，连测 3 次稳定后为准（D29 同类）。 | `ApiPermissionService.java`（check+缓存）、`AdminAuthInterceptor.java`（apiperm 接入）、远程库 `t_role_api`（3 管理角色 +456 条） | 生效 |
| D30 | 2026-09-01 | 用户问"继续 T17 还是先处理 apiperm 接入" | 调查后判定：**先做 T17（低风险安全收尾），apiperm 接入需先定口径（数据不全，排期）**。<br>**① T17 调试中心生产开关已落地**：后端 `loan.debug-center.enabled`（默认 false）+ `/api/debug/**` 纳入 AdminAuthInterceptor 拦截（WebMvcConfig addPathPatterns 加 `/api/debug/**`）；默认关 → 所有角色（含 SUPER/SUPER_ADMIN）403"调试中心未启用"；开启后仅 SUPER_ADMIN/SUPER 可访问。前端 debug 路由加 `meta.permission='page:debug'`（配合 ROLE_PERMISSIONS：超管两级 ['*'] 放行、其余 403）+ Layout 菜单按 `VITE_DEBUG_CENTER==='true'` 隐藏 /debug 项（生产 build 不泄漏入口）。**验证**：BOSS/SUPER/SUPER_ADMIN/ADVISER 调 POST /api/debug/match 全部 2001"调试中心未启用"；回归 BOSS 菜单树/ADVISER 线索正常。<br>② **apiperm 接口级权限现状核查（排期依据）**：`t_api_permission` 212 个 ACTIVE 接口齐全（apiKey 前缀完整：lead/order/product/ocr/mini/channel 等）；`t_role_api` **仅 ADVISER(49)+DEPT_MANAGER(74) 配了授权**，BOSS 走 superRole 特判（不落库），**OPERATOR/SUPER_ADMIN/SUPER/CHANNEL 无授权数据**；ApiPermissionService 为**网关设计**（规则 JSON 写 Redis `loan:api-perm:rules` 供网关读取），应用内无本地校验方法、未接入拦截链路。<br>③ **apiperm 接入前置（待用户拍板）**：补齐 5 角色 t_role_api 授权数据（口径：按菜单矩阵推导"角色可见页面→接口前缀"？还是逐接口勾选？）+ 确认 ADVISER/DM 现有 49/74 条口径（含 auth:login/health 等基础接口）。**风险**：数据不全接入=正常用户全拦（重蹈 D29 F-A 403）；口径错误=误拦业务。**当前不接入状态**：ADVISER 等可直敲 sms/reward/approval 等无码级接口（页面级模型限制，D29 已标注）。 | `WebMvcConfig.java`（+`/api/debug/**`）、`AdminAuthInterceptor.java`（debug 开关+DEBUG_ROLES）、`router/index.js`（page:debug）、`layout/Layout.vue`（VITE_DEBUG_CENTER 隐藏） | 生效 |
| D29 | 2026-09-01 | 用户反馈"前端页面当前请求切换无法正常使用"（D28 五项落地后） | 端到端复测定位 4 个真实运行时缺陷并全部修复（终验通过）：<br>① **F-A 顾问被管理端拦截器排除（核心）**：`AdminAuthInterceptor.ADMIN_ROLES` 原缺 ADVISER → 顾问登录后菜单 7 项可见但所有接口 403（code:2001"当前角色无权访问管理端"）——交叉评审早已发现的"顾问被排除出 admin 拦截器"遗留 bug。修复：ADMIN_ROLES 加 ADVISER（D19/D20 种子本给顾问 7 项本人视角菜单）。<br>② **F-B 码级校验 URI 未剥离 context-path**：请求 URI 带 `/loan` 前缀，`startsWith("/api/admin/org/")` 永不匹配 → T9 码级校验形同虚设。修复：先 `path = uri.substring(contextPath.length())` 再匹配（渠道白名单/菜单树例外的 endsWith 不受影响）。<br>③ **F-C 渠道登录 5000**：`t_channel_user.phone` 为 AES typeHandler 写入加密列，channelLogin 原 `updateById` 全字段写回 → 密文嵌套累积超长（id=2 达 236 字符）→ `Data too long for column 'phone'`。修复：改 `LambdaUpdateWrapper` 仅更新 `last_login_time`（不触碰 phone/password）+ 远程库重置 id=2 phone 为明文（阶段一明文约定，与 t_staff 一致）。<br>④ **F-D OCR 落盘 FileNotFoundException**：`Paths.get("./uploads")` 相对路径在 Servlet 容器解析到 Tomcat work 临时目录。修复：`OcrController`/`MiniUploadController` 落盘改 `toAbsolutePath().normalize()`。<br>**终验（新进程）**：ADVISER 线索/工单/OCR 连测 3 次全 code=0、菜单 7 项（DM 20/BOSS 26/CHANNEL 3）；ADVISER/DM 的 org+blacklist=2001（码级拦截，F-B 修复后真正生效）、BOSS org/blacklist=0、DM audit=0（工作台复用）；渠道登录 code=0、产品 GET=0（本行隔离）、POST=2001；OCR 用 curl 标准 multipart 验证 code=0（ocrRecordId=3 落库；此前 Python 手工 multipart 5000 为测试构造问题，前端 el-upload 标准 FormData 无忧）。**遗留标注**：ADVISER 无 page:audit 但仍可调 audit/page（工作台复用所需，前端 meta 管控）；ADVISER 可调 sms/reward/approval 等无码级接口（页面级模型限制，直敲 URL 可触达，接口级权限为后续项）；apiperm 模块（t_api_permission）未接入拦截链路（独立模块）。 | `AdminAuthInterceptor.java`（ADMIN_ROLES+context-path）、`AuthService.channelLogin`（LambdaUpdateWrapper）、`OcrController.java`/`MiniUploadController.java`（绝对路径）、远程库 t_channel_user（phone 重置） | 生效 |
| D28 | 2026-09-01 | 剩余待办按序执行（T9/T10/T11 子项/T13/T16，用户"按顺序执行并测试完成"） | 用户要求读取 skills 严格执行 → 已加载 change-safety-check（L4 结论核对）+ doc-hygiene，先读知识库再动手。五项全部落地并端到端验证：<br>① **T9 后端·拦截器码级校验**：AdminAuthInterceptor 注入 MenuMapper/RolePermissionMapper，URI 前缀→t_menu.permission_code（`/org/`→page:org、`/blacklist/`→page:blacklist、`/client/`→page:client）；权限码未配置时保守放行防误伤；**audit 前缀不做码级拦截**（工作台复用 audit/page 且 ADVISER 无 page:audit，由前端菜单+meta 管控）；**顺带修复 T9 前端遗留**：client 路由 meta.permission='client'→'page:client'（原与 ROLE_PERMISSIONS 不匹配，DM/ADVISER 打开客户档案会被误拦）。<br>② **T10 后端结构就绪**：t_menu 加 `customer_group` 列 + `idx_customer_group`（schema.sql + 远程库 ALTER 已执行）；Menu/MenuNodeVO 加 customerGroup + OrgService 透传（前端 6合3 单入口，cg 供未来按客群授权）。<br>③ **T11 子项·渠道产品 Web**：LoanUser 加 bankChannelId/bankChannelCode（channelLogin 按 t_channel_user→t_bank_channel 填充）；拦截器渠道白名单放行 GET `/api/admin/product/page`；ProductController 对 CHANNEL 强制按本行 bank_channel_code 硬隔离（无编码返回空集防越权）；**修复 schema 真源漂移**：t_bank_product.bank_channel_id→bank_channel_code、t_partner_product 补 bank_product_code+uk。<br>④ **T13 图标集中 registry**：新建 `components/AppIcon.vue`（24×24 SVG registry，28 图标名统一线性描边）；Layout.vue 33 处内联 SVG 收敛为 name 引用；清理 3 处失效 svg !important 尺寸规则（scoped+v-html 失效根除）。<br>⑤ **T16 Web OCR**：OcrController 加 POST `/api/admin/ocr/recognize`（MultipartFile 落盘 loan.upload.base-dir + 复用 OcrService.recognize）；前端新建 views/ocr/OcrCenter.vue + api/ocr.js；路由 /ocr + 菜单"材料识别"（客户经营组）+ 种子 27 项（6 角色授权，CHANNEL 除外）。<br>⑥ **渠道登录运行时 bug 修复**：channelLogin 原 updateById 全字段写回，因 ChannelUser.phone 带写入 typeHandler（AES）会把密文再加密导致 `Data too long for column 'phone'` → 改为精确更新仅 last_login_time。<br>**验证**（后端编译 exit 0；前端 6 文件 node --check 全过；前端 27 path=种子 27 零差异；远程库种子执行成功：BOSS 26/OPERATOR 26/SUPER_ADMIN 27/SUPER 27/DM 20/ADVISER 7/CHANNEL 3）：<br>码级校验 BOSS org=0（放行）、ADVISER org/blacklist=2001（拦截）、DM/ADVISER client=0、DM audit=0、ADVISER audit=0（工作台复用不拦）、渠道 audit/POST product=2001、渠道 GET product/page=0（仅本行 WH_BANK 隔离）、BOSS OCR=0（ocrRecordId 落库）、渠道 OCR=2001；BOSS 菜单树 26 项含 customerGroup 与 /ocr、渠道菜单树 3 项沙箱。<br>**待办清单更新**：T9/T10/T11 子项/T13/T16 全部 ✅，仅剩 T17 生产开关（调试中心）一项。 | `AdminAuthInterceptor.java`、`AuthService.java`、`LoanUser.java`、`ProductController.java`、`ProductQueryService.java`、`OrgService.java`、`Menu.java`、`MenuNodeVO.java`、`OcrController.java`、`router/index.js`、`components/AppIcon.vue`、`views/ocr/OcrCenter.vue`、`api/ocr.js`、`layout/Layout.vue`、`db/loan-db-schema.sql`、`db/init-data.sql`、`db/menu-permission-seed-2026-09-01.sql`、远程库（ALTER+种子） | 生效 |
| D27 | 2026-09-01 | D26 修复执行（用户"现在执行"） | D26 四项修复全部落地：<br>① **P0** ALTER 远程库 `t_ocr_record` 补 `biz_code`/`updated_at` 列（预检无数据后执行；实体 OcrRecord 与表对齐，OCR 链路 1054 风险消除）；<br>② **P1** `t_channel_user_list` 加 `uk_channel_cg_type_key(channel_code,customer_group,list_type,list_key)`、`t_bank_contact` 加 `uk_channel_phone(bank_channel_id,phone_hash)`（均先查重：无重复）；<br>③ **P1** `loan-db-schema.sql` 补录 7 张缺失表 DDL（t_api_permission/t_role_api/t_sensitive_view_grant/t_sensitive_view_log/t_notification/t_channel_user_list/t_bank_product_city，SHOW CREATE TABLE 导出、去 AUTO_INCREMENT，插入种子区前）→ **schema 75 表与远程库 75 表完全对齐**；<br>④ **P2** `03-数据模型` 文档 4 处修正（t_client→t_client_profile；owner_staff_id→owner_staff_code；t_ocr_record 列名；t_staff deptId→dept_code）。<br>验证：后端 health code:0 + 前端 5173=200 无影响。数据库设计现**判定满足历史结论要求**。 | 远程库 loan_db（3 处 ALTER）、`db/loan-db-schema.sql`（+7 表）、`03-数据模型（DB schema 索引）.md` | 生效 |
| D26 | 2026-09-01 | 数据库设计 vs 历史结论符合性审查（用户要求结合历史结论检查） | 对照 C2/C4/C5/C19/C20/C21/C22 与 03 索引、C19 实施方案，审查远程库 loan_db（75 表）vs schema.sql（68 表）：<br>**✅ 核心结论表全部在位且结构正确**：t_screening_product（7 列+uk_report_product）、t_client_allocation_approval（9 列+uk_approval_no）、t_industry_benchmark（18 列+uk 复合）、t_client_profile（owner_staff_code+hash 三件套+uk×3）、t_lead/t_lead_ent_ext（C20 渠道走 Lead）、t_client_submission.data_json（诊断数据源）、t_client_screening.uk_report_no（C4 关联）；敏感字段 AES+SHA 全库普及；业务唯一索引普遍。<br>**⚠️ 待修复**：① **P0 t_ocr_record 缺 biz_code/updated_at 列**（实体 OcrRecord 有 bizCode/updatedAt，映射必报 Unknown column，OCR 链路运行缺陷，ALTER 补列）；② **P1 t_channel_user_list 无唯一索引**（应加 uk_channel_cg_type_key）、t_bank_contact 无唯一索引（建议 uk_channel_phone）；③ **P1 schema.sql 缺 7 张表真源**（t_api_permission/t_role_api/t_sensitive_view_grant/t_sensitive_view_log/t_notification/t_channel_user_list/t_bank_product_city，需补录）；④ **P2 03 文档 3 处过时**（t_client→t_client_profile；owner_staff_id→owner_staff_code；t_ocr_record 列名）。<br>**观察**：t_report 不存在符合设计（报告体系=t_client_screening+t_screening_product+t_report_template）。修复待用户确认执行（ALTER 远程库 + schema 补录 + 文档修正）。 | `artifacts/数据库设计符合性审查-2026-09-01.md`、`t_ocr_record`、`t_channel_user_list`、`t_bank_contact`、`loan-db-schema.sql`、`03-数据模型（DB schema 索引）.md` | 生效 |
| D25 | 2026-09-01 | 沙盒网络误判纠正 + 环境跑通（用户质疑"其他工具都能连上"） | 用户质疑后复测发现：**之前用 `/dev/tcp` 探测是错误方法**（沙盒 /dev/tcp 不可靠），改用 **Python socket** 实测：远程 MySQL `110.42.219.5:9306`（收到 MySQL 8.2.0 握手包）、Redis `124.221.116.28:9379`、Nacos `124.221.150.239:9848/8848` **全部可达** → 之前"远程库从沙盒不可达、物理上起不来"的结论**错误**，向用户致歉并纠正。<br>**本轮环境落地（后端已在沙盒跑通，直连远程 prd Nacos + 远程库，未启任何本地 Docker）**：① 远程库缺 3 张表（`t_industry_benchmark`/`t_screening_product`/`t_client_allocation_approval`，旧 schema 未建）→ 已从 schema 提取 DDL 补建（注意：提取 DDL 用**按行切分**，不能按分号——表级 COMMENT 内含分号会截断）；② 种子脚本已在远程库**执行成功**（修掉 t_menu 残留 permission_code 值后整体 MULTI_STATEMENTS 执行；t_menu 26+1 旧 /report 遗留行、7 角色权限正确：BOSS 25 / OPERATOR 25 / SUPER_ADMIN 26 / SUPER 26 / DM 19 / ADVISER 6 / CHANNEL 3）；③ `OrgService.listMenusByRole` **移除 BOSS 全放行特判**（BOSS 也统一走 t_role_permission，调试中心仅 SUPER/SUPER_ADMIN 由权限表控制，D24 设计落地）。<br>**验证通过**：BOSS 菜单 25 项（无 /debug）/ 渠道登录（13911112222+模拟串）沙箱菜单 3 项 / DM 菜单 19 项（无 org/blacklist）；前端 5173 就绪。<br>**教训**：网络可达性探测优先用 Python socket/真实协议握手，勿用 /dev/tcp。 | `loan-service/.../org/service/OrgService.java`、`db/menu-permission-seed-2026-09-01.sql`、远程库 loan_db（补 3 表 + 种子） | 生效 |
| D24 | 2026-09-01 | t_staff 部门列真源漂移修复（用户报 1054 Unknown column 'dept_id'） | 用户执行增量种子报 `Unknown column 'dept_id'`，贴出实际库 `t_staff` 结构为 **`dept_code` varchar(32)（业务编码）** → 证实仓库 `loan-db-schema.sql` 的 t_staff 定义（`dept_id` bigint）为**过时定义**，与实体 `Staff.deptCode`（`staff/entity/Staff.java:38`，MyBatis 映射 dept_code）、`t_department.dept_code`（uk_dept_code）、实际运行库**三方不一致**。<br>**修复（三处对齐 dept_code 业务编码）**：① `loan-db-schema.sql` t_staff `dept_id`→`dept_code`（含 idx_dept_role）；② `db/init-data.sql` t_staff 列与值（1/2/4 → BOSS_DIRECT/CONSULT/OPERATION）；③ 增量脚本 t_staff 加 `dept_code` 列 + ON DUPLICATE 同时更新 role_code/dept_code（重复执行可补全部门）。代码侧 grep 零引用 dept_id/getDeptId（全走 deptCode）✅。<br>**遗留待查**：schema.sql 中 `t_dept_approver` 仍用 `dept_id`（独立表，本次未动）；注释「数据范围隔离走 dept_id + role_code」疑似过时表述，后续核实。<br>**教训（规则 9 范畴）**：增量脚本面向任意版本已有库，只写基础必填列、避免版本敏感可空列；schema 与实体/实际库漂移时必须对齐真源。 | `db/loan-db-schema.sql`、`db/init-data.sql`、`db/menu-permission-seed-2026-09-01.sql` | 生效 |
| D23 | 2026-09-01 | 菜单重构剩余项落地（T9前端/T10前端/T12/T14/T15/T17） | 用户"继续未完成待办"整体授权 → 本轮落地：<br>① **T9 前端**：ROLE_PERMISSIONS 对齐菜单矩阵（DEPT_MANAGER=page:client/audit、ADVISER=page:client），4 条 meta.permission 不再误拦 DM（client/audit 可进，org/blacklist 仅全量角色）；后端拦截器码级校验待排期。<br>② **T10 前端（6合3）**：Layout 菜单企业/个贷 6 项合并为 3 项单入口（/plan-edit /strategy-template /rule，页内切换）；RuleList/StrategyTemplateList 加 el-segmented + URL ?cg= 同步（PlanEdit 已有）；后端 t_menu customer_group 字段待排期。<br>③ **T12**：动态菜单空树收口——成功加载但角色无菜单 → 仅工作台 + 提示条（menu-no-perm），不再回退全集防泄露；加载失败（网络）仍回退全集容错。<br>④ **T14**：分组标题与子项层级差异化（600 字重 + 顶距 + 字距）；激活竖条修复（menu-group-title 补 position:relative + 去 overflow:hidden，原竖条渲染失败）。<br>⑤ **T15**：浅色主题激活态对比度（color-mix(primary 62%, #000) 深蓝字，2.8:1 → ≥4.5:1）。<br>⑥ **T17**：Workbench 移除调试中心快捷入口（菜单可见性由种子控制：仅 SUPER/SUPER_ADMIN；生产开关待排期）。<br>验证：5 文件 node --check 全过；前端 26 去重 path 与种子 26 条仍零差异。<br>**剩余**：T9/T10 后端部分（拦截器码级校验、t_menu customer_group）；T11 子项（渠道产品 Web）；T13（图标 registry）；T16（Web OCR）；T17 生产开关。 | `loan-web/src/layout/Layout.vue`、`store/user.js`、`views/rule/RuleList.vue`、`views/plan/StrategyTemplateList.vue`、`views/Workbench.vue`、`artifacts/菜单重构待办清单-2026-09-01.md` | 生效 |
| D22 | 2026-09-01 | T11 渠道沙箱主体落地（用户确认"能连数据库 + 走 SSO 模拟登录"） | 用户答复①数据库能连（沙盒实测仍 TCP 不可达，种子脚本由用户本机执行）；②渠道登录走 SSO 模拟登录 → 落 T11 主体（后端 + 前端）：<br>① `AdminAuthInterceptor` 渠道白名单：CHANNEL 仅可访问 菜单树/线索创建/本人线索分页，其余 `/api/admin/**` 一律 403；<br>② `LeadController.page` 渠道强制本人隔离（pool 强制 false、ownerNo=本人，防查公海越权；create 本就 owner=本人）；<br>③ `AuthService.channelLogin` **阶段一模拟旁路**：密码约定串 `loan-sim-pwd` 跳过 RSA 解密 + BCrypt（账号仍须存在且 ACTIVE）——与员工 SSO 模拟"密码暂不校验"（Login.vue placeholder）语义一致；**⚠️ 上线前必须移除**；<br>④ 种子补 t_channel_user 示例账号（13911112222 / 渠道-陈 / bank_channel_id=1 / 密码 BCrypt）双真源同步；<br>⑤ 前端：Login.vue 加"员工/渠道"登录 tab（渠道固定传模拟串）、api/auth.js 加 channelLogin、store 抽 `applyLogin` 共用（员工/渠道）。<br>编译 exit 0 + node --check 全过。<br>**剩余**：T11 子项"渠道我的产品 Web 能力"（`PartnerProductController.requireStaff:139-140` 拒绝渠道，渠道产品本在小程序 pages/product；如需 Web 需放开 + bank_channel 硬隔离）；T9/T10/T12 + P2 T13-T17。 | `loan-service/.../auth/service/AuthService.java`、`infrastructure/interceptor/AdminAuthInterceptor.java`、`lead/controller/LeadController.java`、`db/init-data.sql`、`db/menu-permission-seed-2026-09-01.sql`、`loan-web/src/views/Login.vue`、`api/auth.js`、`store/user.js`、`artifacts/菜单重构待办清单-2026-09-01.md` | 生效 |
| D21 | 2026-09-01 | Web 菜单重构 前端 P0-A + P1 前端落地（override D0-1） | 用户"继续完成待办"整体授权，前端 P0-A（T1-T6）与 P1 前端（T7/T8）由助手直接落地（override D0-1，同 ②④/①③⑥ 先例）：<br>① **新 IA 7 组落地**：`Layout.vue` BASE_MENU_GROUPS 重写为 客户经营/匹配与规则/服务与审批/运营与激励/数据与报表/产品与渠道/系统管理（+常驻工作台）；工单+审批同组补 G4 断链；报告模板移入数据与报表；审计移入运营与激励；渠道 5 项归产品与渠道；孤儿路由 /report/trend、/report/screening 上菜单；/report/overview 移出菜单（路由保留）。<br>② **改名 15 项**：Layout 菜单 title + router meta.title ×14 + Workbench 快捷入口 ×3 + 页面 H2 标题 ×7（脚本断言全过；"我的工作台"→"工作台"等）。<br>③ **B1 修复**：`isActive` 支持 query（fullPath 精确 + path 兜底），6 个 `?cg=` 菜单恢复高亮。<br>④ **B2 修复**：tab 键改 fullPath（企业/个贷拆独立 tab），findTitle 支持 query。<br>⑤ **B3 修复**：/403 改为 Layout 内子路由（保留侧栏）；loadRoleMenu 后按角色过滤残留 tab。<br>⑥ **B4 修复**：request.js 加 403 分支。<br>⑦ **store:41 修复**：permissions 空数组真值 bug。<br>⑧ **T6 CHANNEL 接线**：store/user.js roleCode 按 userType 推导 'CHANNEL'；roleText 补"渠道合作方"；ROLE_PERMISSIONS 补 CHANNEL:[]。<br>验证：node --check 全过；前端菜单 26 个去重 path 与种子 `t_menu` 26 条**完全一致**。<br>**剩余待办**：T9 权限码对齐 / T10 6合3 cg 切换（需 t_menu 加 customer_group）/ T11 渠道沙箱页面级放行（需先确认渠道 Web 登录链路）/ T12 空树兜底收口；P2 = T13-T17（图标/层级/对比度/Web OCR/调试开关）。 | `loan-web/src/layout/Layout.vue`、`loan-web/src/router/index.js`、`loan-web/src/store/user.js`、`loan-web/src/utils/request.js`、`loan-web/src/views/Workbench.vue`、`views/sms/SmsCenter.vue`、`views/config/ConfigurationWizard.vue`、`views/plan/{ChannelUserList,StrategyList,ChannelConfigList,ChannelConfigWizard}.vue`、`views/order/OrderList.vue`、`artifacts/菜单重构待办清单-2026-09-01.md` | 生效 |
| D20 | 2026-09-01 | Web 菜单重构 P0-B（后端+数据）落地 | 用户确认按 D19 方案执行（"继续按照你说的来"），本轮完成**后端 P0-B**：① `AdminAuthInterceptor` 放行"已登录用户获取自身菜单树"（含渠道），其余 `/api/admin/**` 仍按角色门控；② `OrgController.menuTree` 服务端取角色（渠道固定 CHANNEL / 员工空参取登录态 roleCode / 兜底 BOSS）；③ 种子数据补全 **26 条菜单 + 7 角色权限**（BOSS/OPERATOR 25 项不含 /debug，SUPER_ADMIN/SUPER 26 项含 /debug，DEPT_MANAGER 19 项部门视角，ADVISER 6 项本人视角且**非审批人**（D0-4），CHANNEL 3 项沙箱：工作台/线索录入/产品库）；④ t_role 补 4 角色、示例员工补 4 管理角色便于逐角色验收。数据真源双份：`db/init-data.sql`（新库）+ `db/menu-permission-seed-2026-09-01.sql`（已有库增量、幂等可重跑）。编译 exit 0。<br>**待办移交（前端归用户 D0-1）**：P0-A = B1 isActive 支持 query / request 403 分支 / store:41 空数组真值 / 15 项改名 / 孤儿路由上菜单（trend+screening）+ overview 下线 / CHANNEL 前端 roleCode 推导+roleText；P1 = B2/B3 / 权限码对齐 / 6合3 cg 切换（需 t_menu 加 customer_group 字段）/ 渠道沙箱页面级放行（lead/product 管理接口）；P2 = 图标 registry / 层级 / 对比度 / Web OCR 页。详见 `artifacts/菜单重构待办清单-2026-09-01.md`。 | `loan-service/.../infrastructure/interceptor/AdminAuthInterceptor.java`、`org/controller/OrgController.java`、`db/init-data.sql`、`db/menu-permission-seed-2026-09-01.sql`、`artifacts/菜单重构待办清单-2026-09-01.md` | 生效 |
| D19 | 2026-09-01 | Web 菜单六方联合评审（产品/UI/UX/后端/前端/QA，代码实读） | **Web 菜单"混乱"根因三层**：① 前端 IA 组织混乱 + 命名违规（非后端能力问题——27 项菜单后端能力 26 完备 / 1 部分"配置向导"）；② 动态菜单**名存实亡**：`db/init-data.sql:33-43` 仅 5 条菜单且仅 BOSS 有 `t_role_permission` → 除 BOSS 外所有角色前端兜底看全量 27 项，BOSS 反而只见 5 项截断；③ 前后端两套权限不对齐：后端拦截器仅角色级（`WebMvcConfig:25-31` 拦 `/api/admin/**`），前端 4 条 `meta.permission`（audit/org/client/blacklist）自成一套 → DEPT_MANAGER 后端可直调、前端却 403。<br>**CHANNEL 泄露属实**：`OrgService.listMenusByRole`（`org/service/OrgService.java:68-90`）只特判 BOSS，CHANNEL 无权限行 → 空树 → 前端 `Layout.vue:363` 兜底回退全量，且 `AdminAuthInterceptor:37` 直接 403 → 渠道见全量但全不可用。<br>**前端真实 Bug（上线前必修）**：B1 `Layout.vue:401-403` isActive 不比 query → 6 个 `?cg=` 菜单项永远不高亮；B2 企业/个贷共用 tab；B3 `/403` 为顶层路由全屏丢侧栏 + loadTabs 无权限过滤；B4 `request.js` 无 403 分支；store/user.js:41 空数组真值 bug（管理员刷新可能自 403）。<br>**OCR 澄清**：后端 `ocr/` 9 类基本完备（`MiniMaterialService:71` 回灌闭环），缺 Web 上传/识别管理端接口（⚠️ 部分）+ 前端页面，勿判为后端大缺口。<br>重构方案（PM）：7 组新 IA + 15 项改名 + 企业/个贷 6 合 3 页内 cg 切换 + 孤儿路由上线（/report/trend、/report/screening）+ /report/overview 下线 + CHANNEL 沙箱 3 项菜单。实施待用户拍板（前端 P0-A 归用户 / 后端 P0-B 归助手，D0-1）。 | `loan-web/src/layout/Layout.vue`、`loan-web/src/router/index.js`、`loan-web/src/store/user.js`、`db/init-data.sql`、`org/service/OrgService.java`、`infrastructure/interceptor/AdminAuthInterceptor.java`、`artifacts/菜单六方评审结论-2026-09-01.md` | 生效 |
| D18 | 2026-08-31 | 三端改造 ①③⑤⑥ 落地 + 5/6 方案确认 | 用户对《三端功能缺失与角色权限盘点》E 节 6 项中此前未落地的 ①③⑤⑥ 授权助手一并实现，并确认 ⑤/⑥ 方案：报表按角色可见范围（老板/运营/超管/超级=全量，部门经理=本部门，顾问=本人）。本轮落地：① Web 补 OPERATOR/SUPER_ADMIN/SUPER 三角色建模 + 动态菜单（接通 `org/menu/tree`）；③ 守卫 deny-by-default（`hasPerm` 未授权即拦截）；⑤ 报表后端角色过滤（`REPORT_FULL_ROLES` + `buildOwnerScope`，编译通过）；⑥ ReportCenter.vue 并入报表中心 + 接路由 + 动态菜单按角色显隐。前端 ①③⑥ 由助手直接落地（override D0-1，同 ②④ 先例）。<br>**SUPER 独立角色真值校正**：后端 `AdminAuthInterceptor.java:31` 确认 STAFF 管理角色集 = BOSS/DEPT_MANAGER/OPERATOR/SUPER_ADMIN/SUPER，**SUPER 是独立于 SUPER_ADMIN 的第五个管理角色**（非别名）；前端三角色建模已含 SUPER，与 D0-4 / F6 一致。 | `loan-web/src/store/user.js`、`loan-web/src/layout/Layout.vue`、`loan-web/src/router/index.js`、`loan-service/.../report/service/ReportService.java`、`report/controller/ReportController.java`、`docs/plans/三端改造实施规格-2026-08-31.md` | 生效 |
| D17 | 2026-08-31 | 三端改造 6 项确认 + 后端落地 | 用户对《三端功能缺失与角色权限盘点》E 节 6 项的确认与落地：① Web 补 OPERATOR/SUPER_ADMIN/SUPER 建模 + 动态菜单（接通 `/api/admin/org/menu/tree`）；② ALLOCATION 独立 table；③ 守卫改为 deny-by-default（声明权限不满足即拦截，未声明放行——用户原答"不清楚你说的"，已澄清采纳）；④ H5 微信 JS-SDK **需要接入**；⑤ 报表中心 **接入，按角色赋予查看权限**；⑥ ReportCenter.vue 并入报表中心。**执行分工（D0-1）**：后端 loan-service 由助手实现、前端由用户实现。<br>**后端已落（本轮）**：`ApprovalController` 分配审批三接口 + `unifiedAudit(ALLOCATION)` 挂 `MiniRoleGuard`（满足 D0-4，不含 DEPT_MANAGER）；新建 `WxJsSdkService`/`WxJsSdkController` + `WxProperties.oaAppid/oaSecret` 提供 JS-SDK 签名（`GET /api/mini/wechat/jssdk/signature`）。<br>**关键结论（微信）**：JS-SDK 的 `jsapi_ticket` 由**公众号**凭据换取，与小程序 appid/secret 是**两套不同凭证**，H5 必须配 `wechat.oaAppid`（错用小程序 appid → invalid signature）。<br>**待定口径（阻塞 ⑤/⑥ 后端）**：报表按角色的数据可见范围规则（老板全量/经理本部门/顾问本人？）需用户拍板后再落过滤。 | `loan-service/.../approval/controller/ApprovalController.java`、`infrastructure/wechat/*`、`docs/plans/三端改造实施规格-2026-08-31.md` | 生效 |
| D16 | 2026-08-31 | 三端角色权限盘点（代码实读结论） | **小程序前端已正确实施 D0-4**：`pages/mine/mine.vue:182-185` `isApproverRole=['boss','operator','super']` 已排除 `deptmgr`；渠道 4 tab 与 D13 一致（`components/TabBar.vue:47-54`）；`store/user.js` 7 角色推导完整。<br>**Web 前端角色权限模型存在缺口（与 D0-4 冲突）**：① 菜单静态（`layout/Layout.vue:268-341` menuGroups），所有登录角色同套菜单，无角色过滤；② `store/user.js:14-19` `ROLE_PERMISSIONS` 仅建模 BOSS/DEPT_MANAGER/ADVISER，OPERATOR/SUPER_ADMIN/SUPER 缺失；③ ALLOCATION 分配审批入口缺失（审批中心仅 PRODUCT/DOWNLOAD 两 tab，`views/approval/ApprovalCenter.vue:10-13`）；④ 守卫默认放行（`directives/permission.js:8` checker=()=>true、`store/user.js:90` 空数组放行）；⑤ 按钮级权限未落地（`OrgCenter.vue:411` permissionCodes 恒空）。<br>**H5**：与小程序同源（`#ifdef H5`），仅 TabBar 宽屏限宽 + 登录降级 mock code（`utils/wx.js`），微信 JS-SDK 分享/支付/企微活码未接入源码。 | `loan-mini/store/user.js`、`loan-mini/components/TabBar.vue`、`loan-mini/pages/mine/mine.vue`、`loan-web/src/layout/Layout.vue`、`loan-web/src/store/user.js`、`loan-web/src/views/approval/ApprovalCenter.vue` | 生效 |
| D15 | 2026-08-31 | **规则 9 正式文本（含三类例外）· 唯一真源** | 规则 9「禁止写死易腐化的瞬时状态」的**唯一权威表述**；各文档/技能一律引用本条，**不得各自表述**。<br>**规则正文**：凡是会随代码或时间变化的值（表数、tabBar 数量、台账最大编号、非锚点 commit hash 等），一律写「**指向真源 + 取值命令**」，不写具体数值。例：写「以 `db/loan-db-schema.sql` 为准」，不写「68 表」；写「从最新条目往下读」+ `grep -o "^| D[0-9-]*" <台账> \| head -1`，不写「从 D14 往下读」。<br>**三类例外（必须保留原值，禁止抽象化）**：<br>① **恢复锚点** —— 如 D8 的 git 基线 `b80c965a`。反证：抽象成"最新 HEAD"后，删除文件再提新 commit 时该 HEAD 已不含被删文件，**回滚能力归零**；<br>② **纠错证据** —— 如 D13「`01` 文档写 3 tab 是错的、实测 4 tab」。抽象掉，"文档错了"即失去证据支撑；<br>③ **已执行动作记录** —— 如 artifacts 归档 9 个文件、D10 重号修复，是对"做过什么"的历史留痕。<br>**判据**：改这个值，读者是「**取到更新的值**」（→ 抽象）还是「**永远丢失一个不可逆事实**」（→ 保留）？拿不准就问用户，不得自行判断。<br>**事故记录**：2026-08-31 曾按规则 9 做清理时误删 D8 恢复锚点与 D12 行号记录，均已修复（D8 由 team-lead 写回、加注禁止抽象化原因；D12 由执行工程师自查后自纠）。<br>**根因**：当时技能文件里写的是无例外版本「（禁止在任何文档中写死该数值）」，导致错误被固化成规范并扩散至 5 处——**规则表述必须自带例外，否则照做即出错**。 | 所有文档、`docs/skills/**`、`.workbuddy/skills/**`、用户级技能 | 生效 |
| D14 | 2026-08-31 | **D10 语义澄清（"清理"的唯一定）** | **D10 中"纳入清理"一词存在歧义，本条为其唯一解释：「清理」= 归档（移动 + 批量改引用），不是删除。** 适用范围：`docs/` 根目录 5 份游离文档。<br>① `方案评审定稿纪要.html` 移入归档目录后**必须批量改指其 25 处引用**（分布于 12 个文件，其中 4 个 SKILL.md 视其为"唯一有效契约基线"），确保零死链、保住 D6 已改的 68 表成果；<br>② 其余 4 份（`channel-admission-v2-design.md`、`小程序H5落地清单-2026-08-30.md`、`ui-*.md`×3）一并归档；<br>③ `业务ID规范.md` 与技能 `loan-biz-id` 的**重复真源必须消重**（其被引用 4 处）。<br>**理由**：直接删除会一次性制造 25 条新死链（比修复前的 7 条更糟），且直接违反 T05 验收标准「死链清零」。 | `docs/方案评审定稿纪要.html`、`docs/业务ID规范.md`、4 个 SKILL.md、`docs/plans/` 4 份、`docs/knowledge-base/README.md` | 生效 |
| D13 | 2026-08-31 | 渠道 tabBar 数量（文档纠错） | `01-角色权限模型.md` 写"渠道 3 tab"，与代码不符。以 `loan-mini/components/TabBar.vue` 为准，渠道为 **4 tab**（首页·我的产品·录入客户·我的），01 文档待修正。 | `01-角色权限模型.md`、`.workbuddy/skills/role-channel/SKILL.md` | 生效 |
| D12 | 2026-08-31 | artifacts 归档缺口（死链修复） | README 与 06 引用的 `artifacts/` 5 个文件在仓库中不存在，实际位于外部会话目录 `/Users/admin/WorkBuddy/2026-08-25-14-58-50/.workbuddy/artifacts/`。**决策：cp 归档进 `docs/knowledge-base/artifacts/`（扁平、保留原文件名），不保留绝对路径**；png 截图与 V2 过期版本不归档。已执行 9 个文件（cp 非 mv，外部目录保持完好），README 5 条 artifacts 引用 **5/5 解析成功**，`小程序H5落地清单` **:26/:44** 两处引用已改指新路径（**已执行动作记录，按规则 9 例外条款保留原值**；行号会随后续编辑漂移，重新定位请用 `grep -rn "artifacts/" docs/`）。<br>【编号说明】本条初写为 D10，与同期并发写入的「清理范围扩大（E2）」重号，按"只改自己条目"原则改编号为 D12。 | `docs/knowledge-base/artifacts/`、`README.md:41,50-53`、`06` 第 09/C14 行、`docs/小程序H5落地清单`:26,44 | 生效 |
| D11 | 2026-08-31 | 知识治理体系设计（索引与技能架构） | 文档索引从编号制改为"按业务域"的索引层（G1-G9，编号文件 00-09 保留不动）；技能迁至项目级 `.workbuddy/skills/`（21 个）+ 用户级 `~/.workbuddy/skills/`（3 个）；业务 7 角色 + 团队 4 角色两套技能；角色技能一律以代码为准（如 ALLOCATION 审批不含 DEPT_MANAGER）。设计文档：`docs/plans/知识治理体系设计.md` | `docs/knowledge-base/README.md`、`11-业务域文档索引.md`、`index/*.json`、`.workbuddy/skills/**`、`docs/skills/**`（迁移后删） | 生效 |
| D10 | 2026-08-31 | 清理范围扩大（E2） | `docs/` 根目录 5 份游离文档**本轮一并纳入清理**（用户拍板，非架构师原建议的"下一轮"）：`方案评审定稿纪要.html`、`channel-admission-v2-design.md`、`小程序H5落地清单-2026-08-30.md`、`业务ID规范.md`、`ui-*.md`×3。其中 `业务ID规范.md` 与新建技能 `loan-biz-id` 疑为**重复真源**，必须消重——避免刚建完体系就留下双真源隐患。 | `docs/` 根目录 5 份、`docs/skills/loan-biz-id/`、`docs/knowledge-base/02-业务红线*` | 生效 |
| D9 | 2026-08-31 | 表数缺口处置（E8） | `docs/方案评审定稿纪要.html` 第 16 章域级明细加总仅 65，与真实 68 张**差 3 张未定位到业务域**。**不单开人工核对轮次**，交由一致性校验脚本新增检查项自动输出「schema 有而纪要未提及」的表名清单（该项标记 `WARN`，**不计入退出码**，否则脚本将永远返回 1）。索引统一写「以 `db/loan-db-schema.sql` 为准（当前 68 张 `t_` 表）」，不引 65。 | `db/loan-db-schema.sql`、`docs/方案评审定稿纪要.html` 第16章、`scripts/check-kb-consistency.py` | 生效 |
| D8 | 2026-08-31 | 删除安全基线 | 物理删除前必须先把 docs/ 全量 `git commit` 建立可恢复快照；`docs/plans/` 此前未纳入 git，删除不可恢复。已执行基线提交 **`b80c965a4837dfcf2fe2249728963980f8a0c54c`**（**恢复锚点，按规则 9 例外条款保留原值、禁止抽象化**：若写成「最新 HEAD」，则删除文件后又提新 commit 时，该 HEAD 已不含被删文件，回滚能力归零）：该次提交含 36 个 docs/ 文件，仅含 docs/ 路径，未混入无关工程改动。 | `docs/` 全量、`docs/plans/`、git 历史 | 生效 |
| D7 | 2026-08-31 | 蓝图文档不存在 | `前端交互逻辑蓝图.html`、`后端/前端逻辑蓝图.html` 经全仓搜索确认**不存在**（从未落盘），禁止再引用。契约真源改为三项：`db/loan-db-schema.sql`（表数以该文件为准）+ `loan-service` 实际代码 + `docs/knowledge-base/`。 | `docs/skills/knowledge-base-retrieval/SKILL.md`、`docs/skills/frontend-development/SKILL.md` | 生效 |
| D6 | 2026-08-31 | 契约基线以现实为准 | `docs/方案评审定稿纪要.html` 中的「65 表」**直接改为 68 表**（用户拍板不加勘误、不保留历史数字）。全库表数以 `db/loan-db-schema.sql` 为准（**本条不锁定具体数值**，避免随 schema 演进腐化；取值命令见 D0-2）。 | `docs/方案评审定稿纪要.html`（:73 / :403 / :522 共 3 处） | 生效 |
| D5 | 2026-08-31 | 知识治理总体要求 | 用户对本项目的知识治理提出 7 条要求：<br>① 整理 docs 和 skills 下最新文档、删除错误逻辑与描述；<br>② 每次把用户对助手说的结论记录下来；<br>③ 形成 skills，每次用户要求修改或调整时都要先检查历史结论；<br>④ 按模块业务逻辑建立文档索引；<br>⑤ 遇到不清楚的必须与用户讨论，不得臆断；<br>⑥ 按系统角色生成各自技能 skills；<br>⑦ 每次用户指出问题时自动更新。 | `docs/` 全量文档、`docs/skills/`、`docs/knowledge-base/`、本文件 | 生效 |
| D4 | 2026-08-31 | 结论日志位置 | 新建 `docs/knowledge-base/10-历史结论与决策日志.md`，按时间倒序追加 D 系列，作为「修改前必查」的唯一决策台账。 | `docs/knowledge-base/10-历史结论与决策日志.md` | 生效 |
| D3 | 2026-08-31 | 纠错方式 | 发现错误逻辑与过期描述 → **直接修正 + 物理删除**（非仅标记）。删除前列出具体路径清单。 | `docs/`、`docs/skills/` 下所有含失效路径/过期描述的文档 | 生效 |
| D2 | 2026-08-31 | 角色体系 | 技能要按**两套角色**都建——业务 7 角色（客户 / 渠道 / 顾问 / 部门经理 / 老板 / 运营 / 超管）+ 团队 4 角色（产品经理 / 架构师 / 工程师 / QA）。 | `.workbuddy/skills/`、`~/.workbuddy/skills/`、`docs/knowledge-base/08-*` | 生效 |
| D1 | 2026-08-31 | 技能存放位置 | **两处都要**：项目级 `loan-main/.workbuddy/skills/`（本项目专属、随 git）+ 用户级 `~/.workbuddy/skills/`（通用工作流习惯）。原因：现有 `docs/skills/` 不在系统加载路径，技能无法被自动加载。 | `.workbuddy/skills/`、`~/.workbuddy/skills/`、`docs/skills/` | 生效 |
| D0-4 | 2026-08-30 | 审批权限真值（来源：代码核查沉淀） | ALLOCATION 分配审批仅 `OPERATOR` / `SUPER_ADMIN` / `SUPER` / `BOSS` 可审（**不含 DEPT_MANAGER**）；PRODUCT / DOWNLOAD 审批才含 `DEPT_MANAGER`。依据代码 `MiniRoleGuard`（`APPROVER_ROLES` vs `APPROVAL_ROLES`）。**本条推翻**了《遗留项增量设计-2026-08-30》中的相反描述。 | `MiniRoleGuard`、`MiniApprovalController`、知识库 08 角色矩阵 | 已被 D39 部分替代（ALLOCATION 已含 DEPT_MANAGER 本团队） |
| D0-3 | 2026-08-30 | OCR 方法真名（来源：代码核查沉淀） | OCR 回灌方法真名为 `SubmissionFactsMerger.mergeFromOcr(reportNo, ocrFacts, operator)`，**不是** `mergeToOcr`。 | `SubmissionFactsMerger`、`MiniMaterialService`、知识库 03 / 06 | 生效 |
| D0-2 | 2026-08-30 | schema 表数（来源：`db/loan-db-schema.sql` 实测） | **记录时点（2026-08-31）实测为 68 张** `t_` 表；`t_industry_benchmark` 原已存在于主 schema（非本次新增）。表数以 `grep -c "CREATE TABLE" db/loan-db-schema.sql` 为准。 | `db/loan-db-schema.sql`、`docs/knowledge-base/README.md`、`docs/plans/` | 生效 |
| D0-1 | 长期 | 分工红线（来源：项目长期记忆） | 前端（loan-web / 小程序）由用户自己负责，助手只做后端 loan-service；定时任务必须用 XXL-Job，禁止 `@Scheduled`；业务 ID = 小写前缀 + 32 位随机。 | `loan-service/` 全部后端代码、`docs/knowledge-base/02-业务红线*` | 生效 |

## 状态说明

- **生效**：当前必须遵守
- **已被替代 → 指向新编号**：被后续结论推翻，保留供追溯
- **已废弃**：不再适用

## 推翻规则

结论被推翻时**不要删除原条目**，改为：状态列写「已被 Dxx 替代」，并在新条目中说明推翻了哪条。

## 追加模板

新增结论时复制到表格**最上方**（最新在上）：

```
| D<序号> | <YYYY-MM-DD> | <场景/触发> | <结论（用户原意，忠实记录）> | <影响文件/模块> | 生效 |
```
