# 00 · 项目结构与代码地图

## 活跃项目根（必读）

> ⚠️ **不要用早期副本** `/Users/admin/IdeaProjects/loan-platform/loan-platform/`（仅 14 个 java，停摆）。

**主项目**：`/Users/admin/Downloads/loan-main/`。文件与模块数量会随实现变化，使用下方真源命令实时获取，不在文档中写死。

```
loan-main/
├── loan-api/                        # 跨模块 DTO / API 契约
├── loan-gateway/                    # 统一鉴权与路由（8088）
├── loan-service/                    # 后端 Spring Boot
│   └── src/main/java/com/loan/
│       ├── mini/                    # 小程序端接口层（C 系列落地）
│       ├── screening/               # 初筛执行服务（ScreeningService）
│       ├── engine/                  # 规则引擎
│       ├── report/                  # 报告域（含 ScreeningProduct 新增）
│       ├── approval/                # 审批（产品/附件/分配 — ClientAllocationApproval 新增）
│       ├── client/ / submission/    # 客户 + 提交单
│       ├── ocr/                     # OCR 提取与回灌（T2 新增 service/impl、model）
│       │   ├── controller/          # OcrController
│       │   ├── service/             # OcrService / OcrExtractor；service/impl（MockOcrExtractor 实现）
│       │   ├── model/               # OcrResult（OCR 回灌模型）
│       │   ├── entity/              # OcrRecord / ExtractFieldDef
│       │   └── mapper/              # OcrRecordMapper / ExtractFieldDefMapper
│       ├── staff/                   # t_staff 实体/Mapper（staffName 来源）
│       ├── auth/ / context/         # 鉴权 + 当前用户上下文
│       └── ...（以源码目录为准）
├── loan-web/                        # Web 管理端（5173，开发代理经 8088）
├── loan-mini/                       # 前端 uni-app（Vue3 + setup）
│   ├── pages/                       # 页面以 pages.json 与 pages/ 为准
│   ├── components/                  # AppButton/AppIcon/AppTag/AppListItem/AppStepper/AppEmpty/AppSkeleton/TabBar
│   ├── store/                       # Pinia（user.js 持有 role/token/profile/authStatus）
│   ├── api/                         # 与后端 /api/mini/* 对应
│   └── App.vue                      # 全局样式 + 无障碍焦点 + H5 hover
├── db/                              # schema + 初始化 / 迁移 SQL
│   └── loan-db-schema.sql
├── scripts/service.sh               # 四端统一启停入口
└── docs/                            # 落地清单 / 方案 / plans / **knowledge-base/**
```

> OCR 回灌链路（T2）：`OcrController` → `mini.service.MiniMaterialService` → `submission.service.SubmissionFactsMerger.mergeFromOcr`（仅补空、写库 `for update` 行锁防并发覆盖）。

## 关键命令

```bash
# 四端状态 / 启动（8080 / 8088 / 5173 / 5174）
cd /Users/admin/Downloads/loan-main
bash scripts/service.sh status
bash scripts/service.sh start all

# 后端编译
mvn -q -pl loan-service -am compile -DskipTests

# Web 构建
cd loan-web && npm run build

# 客户端构建（注意 manifest 在根目录，必须用 npm run 而非 npx uni build）
cd ../loan-mini
npm run build:h5           # H5 端
npm run build:mp-weixin     # 微信小程序端

# 动态事实：表、页面、后端一级包
cd ..
rg -c '^CREATE TABLE' db/loan-db-schema.sql
find loan-mini/pages -name '*.vue' -type f | sort
find loan-service/src/main/java/com/loan -mindepth 1 -maxdepth 1 -type d | sort
```

## 模块地图（阶段四规划）

| 阶段 | 内容 | 当前落地 |
|------|------|---------|
| 阶段一 | 数据模型 / 规则引擎 / OCR / 上下文 / 组织权限 / 线索回收 | ✅ schema 以 `db/loan-db-schema.sql` 为准；引擎跑通 |
| 阶段二 | Web 管理端（工作台 / 权限 / 审批 / 报表 / 客户 / 渠道 / 工单 / 短信 / 报告模板） | **2026-08-30 复核已更新**：order/approval/reward/blacklist/dashboard 从空目录变为有实现；plan/（执行计划/策略模板/渠道名单）为清单外新增域（27 文件）；8 个空占位包（vip/stats/oss/match/job/interceptor/enums/crm）；**管理端 /api/admin/** 已加统一鉴权拦截器（AdminAuthInterceptor）** |
| 阶段三 | uni-app 小程序（登录/认证/匹配/报告/线索/工单/产品/顾问） | ✅ 已落地（小程序端）+ 员工侧闭环修复（C28）；H5 端限宽/焦点/hover 修复（C29） |
| 阶段四 | VIP 付费 / 推送 / CRM 联动 / 奖励结算 | 未开始（reward 部分已实现 DEAL 自动结算） |

## 沙盒常见拦截

| 触发 | 拦截 | 绕过 |
|------|------|------|
| `rm -rf` 大量文件 / `shutil.rmtree` 50+ 文件 | SAFE_DELETE_BULK_CONFIRM | 用 `mv` 移走 |
| `rm -rf` Desktop/Downloads/Documents/Home 等用户目录 | 永远禁止 | 不操作 |
| `find / -delete` / 修改 `/` `/System` | 永远禁止 | 不操作 |
| 大段文本写入（> 50KB）到非工作目录 | 可能拦截 | 分批或在工作目录 |

## 跳到

- 角色权限：`01-角色权限模型.md`
- 红线：`02-业务红线与编码规范.md`
- 数据库：`03-数据模型（DB schema 索引）.md`
- 接口契约：`04-后端 API 契约.md`
- 前端工程：`05-前端工程要点.md`
- 业务结论：`06-业务结论沉淀索引（C1-C19）.md`
