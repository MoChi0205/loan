# 09 · 业务流程知识图谱（结构化数据）

> **定位**：把「分模块交互逻辑与接口设计」与「7 角色功能矩阵」进一步抽象为**节点 + 边**的知识图谱，供业务流程图渲染、影响面分析、程序消费。
> **数据真源**：`09-业务流程知识图谱.json`（机器可读，本文件为其人类可读摘要）。
> **与既有文档关系**：
> - 接口路径/权限以 `04-后端 API 契约.md` + `loan-service` 源码为准（2026-08-30 已校正）。
> - 模块交互细节见 `artifacts/小程序分模块交互逻辑与接口设计.md`（§1–§14）。
> - 结论 C1–C19 见 `06-业务结论沉淀索引` 与 `artifacts/小程序模块结论沉淀.md`。
> - 可视化见 `artifacts/企融通业务流程图.html`（静态版）与 `artifacts/企融通业务流程图-交互版.html`（**交互版**：点击节点联动本图谱详情 + 角色筛选高亮可见/禁入模块，单文件内联本 JSON，可直接浏览器打开）。

## 1. 图谱结构（Schema）

| 节点类型 | 说明 | 实例数 |
|----------|------|--------|
| `roles` | 7 角色（userType+roleCode） | 7 |
| `modules` | 业务模块（对应逻辑文档 §1–§13） | 12 |
| `interfaces` | 真实后端接口（路径以源码和 JSON 数组为准） | 以 JSON 为准 |
| `tables` | 关键数据表（含 T1/T2/T4 新增 + C19 2 表） | 以 JSON `tables` 数组为准 |
| `statemachines` | 状态机（匹配结果/产品/归属流转） | 3 |

| 边类型 | 含义 |
|--------|------|
| `FLOW_SEQ` | 模块间流程顺序（端到端主线） |
| `HAS_API` | 模块拥有接口 |
| `READS` / `WRITES` | 接口读写数据表 |
| `CAN_ACCESS` | 角色对模块的可访问性（含 FORBIDDEN/脱敏） |

## 2. 角色节点（7）

| id | 角色 | userType | roleCode | tab | 匹配 | 报告可见 | 产品可见 | 分配审批 |
|----|------|----------|----------|-----|------|----------|----------|----------|
| R1 | 客户 | CUSTOMER | — | 5 | 仅自己 | 仅自己+仅日期 | ❌脱敏 | — |
| R2 | 渠道 | CHANNEL | — | 4 | ❌禁入 | 小程序隐藏；Web 本人客户只读 | 自有 | — |
| R3 | 顾问 | STAFF | ADVISER | 5 | ✅ | 全量四维 | ✅ | 申请 |
| R4 | 部门经理 | STAFF | DEPT_MANAGER | 5 | ✅ | 全量 | ✅ | 申请 |
| R5 | 老板 | STAFF | BOSS | 5 | ✅ | 全量 | ✅ | ✅审批入口 |
| R6 | 运营 | STAFF | OPERATOR | 5 | ✅ | 全量 | ✅ | ✅审批入口 |
| R7 | 超管 | STAFF | SUPER_ADMIN | 5 | ✅ | 全量 | ✅ | ✅审批入口 |

## 3. 模块节点（12）与主线流程（FLOW_SEQ）

```
M_AUTH ──▶ M_NAV ──▶ M_HOME
                         ├─(客户/员工)─▶ M_MATCH ──▶ M_ALLOC(员工替客:查重→归属流转)
                         │                  └─▶ M_REPORT ──▶ M_PRODUCTS(员工)
                         │                                  ├─▶ M_DIAG
                         │                                  └─▶ M_ORDER
                         └─(渠道)──────▶ M_CHANPROD ──▶ M_ALLOC(删除终审待运营/老板批)
M_INVITE / M_LEAD 为并行支线（不阻塞主线）
```

| 模块 | 文档 | 关键接口 |
|------|------|----------|
| M_AUTH 登录/认证 | §1 | A1/A2/A3/A4 |
| M_NAV 角色化导航 | §2 | —（前端 store.role 驱动） |
| M_HOME 首页 | §3 | — |
| M_MATCH 智能匹配 | §4 | M1/M3/M4 |
| M_ALLOC 审批中心(统一,C22) | §4.3/§13 | M5/M6 + UA1/UA2/UA3（统一审批计数/待审/audit；M7/M8/M9 已废弃改 UA）；白名单 `loan.mini.approval.types=ALLOCATION` |
| M_REPORT 我的报告 | §5 | R1i/R2i |
| M_PRODUCTS 命中产品 | §6 | R3i |
| M_DIAG 经营诊断 | §7 | R4i（READS t_ocr_record；OCR 回灌边 M_MATCH→M_DIAG 仅补空不覆盖） |
| M_ORDER 服务单 | §8 | O1/O2 |
| M_CHANPROD 渠道产品 | §9 | P1–P10 |
| M_INVITE 邀请/奖励 | §10 | I1/I2/I3/W1/W2 |
| M_LEAD 线索录入 | §11 | L1（线索录入）/ L2（我录入的线索，仅本人） |

## 4. 状态机节点（3）

### 4.1 SM_MATCH 匹配结果态 `totalResult`
`PASS` / `CONDITION` / `REJECT` / `SKIP_SEGMENT_MISMATCH` / `ERROR`

### 4.2 SM_PRODUCT 渠道产品状态机（C9，复用 `t_product_approval`）
```
DRAFT ──submit──▶ PENDING ──(终审通过)──▶ OK
  ▲                   │                      │
  └────revoke─────────┘             delete-apply
                                   │
                              PENDING_DELETE ──audit(approve)──▶ 物理删除(软删+审计)
                                   │──audit(reject)/delete-cancel──▶ OK
```

### 4.3 SM_ALLOC 客户归属流转（分享引荐与顾问归属解耦）
```
微信客户注册(owner_staff_code=NULL) ──▶ 未分配客户池
  ├─顾问申请认领(ADVISER_CLAIM)──┐
  └─管理者指定(MANAGER_ASSIGN)────┴─▶ PENDING ──approve──▶ APPROVED(CAS 写入归属)
                                             └─reject(opinion必填)──▶ REJECTED

分享邀请码 ──▶ 引荐关系(referrer)；不写 owner_staff_code，不生成 INVITE 线索
```

## 5. 数据表节点（数量以 JSON `tables` 数组为准）

| 表 | 模块 | 关键字段 / 备注 |
|----|------|------------------|
| t_client_profile | 匹配/客户 | owner_staff_code（NULL=未分配客户池） |
| t_client_submission | 匹配/诊断 | data_json（经营事实，C5 数据源）；client_submit_id 幂等 |
| t_client_screening | 报告 | report_no, total_result, grade |
| t_screening_product | 命中产品(B1) | report_no, product_code, hit_result, match_score |
| t_client_allocation_approval | 分配审批(B3) | approval_no, client_code, applicant_staff_code, approve_status, pending_key, apply_source |
| t_invitation | 分享引荐 | referrer_client_code, used_by_client_code；与顾问归属解耦 |
| t_product_approval | 渠道产品(C9) | apply_type CREATE/DELETE；approve_status 五态 |
| t_lead | 线索(T4) | owner_staff_code(NULL=未预设顾问)；渠道待审/驳回仅本人可见，通过为 NEW 后进公海；recorder_staff_code 保存录入主体 |
| t_staff | 报告/工单(B4) | staff_code, staff_name |
| t_bank_product | 命中产品 | product_code, product_name, bank_name |
| t_bank_channel | 命中产品 | 银行渠道关联 |
| t_industry_benchmark | 行业均值(T1) | dimensions+均值；IndustryBenchmarkService.avgByDimension |
| t_lead_ent_ext | 线索企业扩展(T4) | ent_name/credit_code/industry/found_years/annual_tax_amount/annual_invoice_amount |
| t_ocr_record | OCR 回灌记录(T2) | ocr_record_id/biz_code/extracted_fields/confidence；_ocrMeta.version 派生 materialVersion |
| t_client_business_fact | 客户经营事实 | 诊断/匹配经营指标落库（与 t_client_submission 同源） |
| t_service_attachment | 服务资料附件(M_DIAG) | id/order_no/client_profile_code/report_no(诊断补充材料场景)/attachment_type/file_key/file_name/upload_staff_id/upload_time |

## 6. 关键边（CAN_ACCESS 红线摘录）

- R2(渠道) → M_MATCH / M_PRODUCTS(命中产品明细) / M_ORDER：**FORBIDDEN / 隐藏**；→ M_REPORT：小程序隐藏，Web 仅本人录入客户分析报告只读（D50）。
- R2(渠道) → M_LEAD ：**ALLOWED**（渠道走 Lead 录入，新增后本人立即可见；公司终审通过后进公海，不直接进 Client；见 C20/D50）。
- R2(渠道) → 渠道客户只读：`userNo → t_lead(recorder_staff_code/ext_json) → clientCode → reportNo`，返回归属顾问姓名但不开放归属操作。
- R4(部门经理) → M_ALLOC ：ALLOCATION **不可**审批（仅 OPERATOR/SUPER_ADMIN/SUPER/BOSS）；PRODUCT/DOWNLOAD 含 DEPT_MGR（白名单 `loan.mini.approval.types=ALLOCATION` 当前仅开 ALLOCATION，见 C22）。
- R4(部门经理) 可通过 Web 为未分配客户选择顾问并发起 `MANAGER_ASSIGN`，但仍不能审批自己或他人的 ALLOCATION 单。
- R1(客户) → M_PRODUCTS ：**FORBIDDEN**（对客仅返数量+评级，不返 productName/bankName，合规红线）。
- R1(客户) → M_REPORT ：后端强制**仅自己 + 仅日期**，前端传的跨用户参数一律忽略。
- R5/R6/R7(老板/运营/超管) → M_ALLOC ：拥有**审批入口**（C19 后端 M7–M9 已迁至统一 MiniApprovalController: UA1/UA2/UA3，T5 就绪；小程序 `pages/approval/list.vue` + `mine.vue` 入口已存在，见 08）。
- R4i(诊断) → t_ocr_record ：**READS**（`materialVersion` 由 `data_json._ocrMeta.version` 派生；OCR 回灌仅补空不覆盖，见 C21）。
- M_MATCH ──**OCR回灌**──▶ M_DIAG（T2：上传材料经 `MiniUploadController → MiniMaterialService → SubmissionFactsMerger.mergeFromOcr` 回灌诊断 facts，`mergeFromOcr` 仅补空、写库 `for update` 防并发）。

## 7. 消费方式

- **流程图渲染**：`artifacts/企融通业务流程图.html` 直接依据本图谱的 `FLOW_SEQ` + `statemachines` 绘制。
- **影响面分析**：改某接口 → 查 `HAS_API` 反推模块 → 查 `CAN_ACCESS` 看受影响角色 → 查 `READS/WRITES` 看数据表。
- **程序消费**：读取 `09-业务流程知识图谱.json`，按 `nodes/edges` 直接构图（节点 id 跨文件一致：R*=角色, M*=模块, A/M/R/O/P/I/W/L/U*=接口, T*=表, SM*=状态机）。

## 跳到
- 接口契约：`04-后端 API 契约.md`
- 逻辑设计：`artifacts/小程序分模块交互逻辑与接口设计.md`
- 角色矩阵：`08-小程序角色功能矩阵.md`
- 结论沉淀：`06-业务结论沉淀索引（C1-C26）.md`
